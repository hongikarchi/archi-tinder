# Search Engine Research Index

Research reports on the archi-tinder (make_web) search/recommendation engine.
Written by the research terminal; consumed by the main implementation terminal via `.claude/Task.md` RESEARCH-READY markers.

See `/Users/kms_laptop/.claude/plans/reflective-weaving-seahorse.md` for the research-terminal plan and conventions.

## Reports

| # | Topic | Status | TL;DR |
|---|-------|--------|-------|
| 01 | [Hybrid retrieval: BM25/lexical + pgvector cosine](01-hybrid-retrieval.md) | Ready for Implementation | Add Katz-style RRF hybrid (`tsvector` + pgvector) gated behind `HYBRID_RETRIEVAL_ENABLED`; no extensions required. 8 proposed backend tasks. |
| 02 | [Re-ranking layer](02-reranking-layer.md) | Ready for Implementation | MMR is already our re-ranker; add a **Gemini 2.5-flash setwise re-rank** of ~60 candidates at session completion, flag-gated `GEMINI_RERANK_ENABLED`, off the swipe hot path, ~$0.003/session. Defers per-swipe LLM re-rank, local cross-encoders, and LTR. |
| 03 | [Query understanding: HyDE + session-aware rewriting](03-query-understanding.md) | Ready for Implementation | `research/algorithm.md` already specifies HyDE-style V_initial; live code silently drops it. Recommend restoring V_initial via one extra Gemini call (produces `visual_description`) embedded through the HuggingFace Inference API on the exact 384-dim MiniLM model — same geometry as stored embeddings, respects "no local SentenceTransformers" rule. 10 proposed backend tasks (BACK-QU-1..8 + TEST-QU-1, ALGO-QU-1). |
| 04 | [Diversity methods: MMR λ vs DPP vs submodular](04-diversity-methods.md) | Ready for Implementation | Keep MMR per-swipe but add a **session-aware λ ramp** `λ(t)=λ_base·min(1, \|exposed\|/N_ref)` (one-line change). Replace MMR with **DPP greedy MAP** (Chen 2018 Cholesky-incremental) at session-final top-K, flag-gated `DPP_TOPK_ENABLED` for safe A/B. Rejects facility-location submodular and hard cluster-bucket methods. |
| 05 | [Preference-weight learning: fixed vs bandits](05-preference-weight-learning.md) | Ready for Implementation | Keep Optuna-tuned fixed weights; defer bandits until ≥1K sessions logged. Reframing insight: `pref_vector` does NOT drive per-swipe card selection in analyzing phase (K-Means centroids do) — adaptive-weight-learning ceiling is low. Option B (online EMA on asymmetry ratio, flag-gated) if active experimentation needed. |
| 06 | [Clustering alternatives: K-Means k=2 vs GMM/DBSCAN/spectral](06-clustering-alternatives.md) | Ready for Implementation | N=3-15, D=384 rules out GMM/DBSCAN/spectral (sample-starved). k=1 is the honest null; k=2 overfits as often as it captures bimodality. Recommend **adaptive k∈{1,2} via silhouette score** (`ADAPTIVE_K_CLUSTERING_ENABLED`, threshold 0.15) + orthogonal **soft-assignment relevance** upgrade (`SOFT_RELEVANCE_ENABLED`, softmax over centroid distances). Both <40-line sklearn-only changes. |
| 07 | [Collaborative signals across users](07-collaborative-signals.md) | Ready for Implementation | **Defer** true CF (iALS/LightGCN/PinSage) until `N_projects ≥ 500 AND median likes/project ≥ 20`. Ship Option B now: flag-gated **global-popularity prior** (default weight=0.0, min_exposures=5) blended into `_build_score_cases()` — the one cross-user signal that converges at tens of users. Per-project (not per-user) is the correct CF entity. 9 backend tasks. |
| 08 | [Embedding model choice (text vs multimodal)](08-embedding-model-choice.md) | Ready for Implementation | Recommend **additive multimodal channel** — add a new `image_embedding` column (jina-clip-v2 1024-dim or SigLIP 2 768-dim) on `architecture_vectors` alongside existing 384-dim MiniLM text embedding. Non-destructive, preserves topic 03's HyDE plan, closes the biggest retrieval gap (building images currently never ranked), stays within Make DB ownership boundary. Rejects text-only upgrades until image channel is measured; rejects unified multimodal replacement (caption-tuned vs paragraph-tuned mismatch). |
| 09 | [ANN indexing threshold (HNSW / IVFFlat on pgvector)](09-ann-indexing.md) | Ready for Implementation (Deferred action) | **Do nothing now.** Brute-force cosine on our ~3,465-row corpus is <1 ms — noise relative to the 2 s swipe budget. Vendor benchmarks place the seq-scan knee at ~50K rows. Ship lightweight per-query timing logs so the trigger detects cleanly (p95 > 150 ms or corpus crosses 50K rows). Pre-negotiate an HNSW `CREATE INDEX CONCURRENTLY` playbook for Make DB; choose HNSW over IVFFlat when triggered. |
| 10 | [Convergence detection robustness](10-convergence-detection.md) | Ready for Implementation | Two structural bugs discovered: (1) Delta-V appended only on likes in analyzing phase → window=3 means 3 likes, not 3 rounds; (2) convergence_history not reset on exploring→analyzing transition → first analyzing Delta-V is cross-metric (centroid vs pref_vector). Ship bug-fix (Option A) unconditionally; then **1-D Kalman filter** on Delta-V with credible-interval gating (`KALMAN_CONVERGENCE_ENABLED`) + Prechelt-style patience/min-delta fallback. Reject PELT/BOCD (need N≥30). |
| 11 | [Cold-start seed strategies](11-cold-start-seed.md) | Ready for Implementation | Keep hard-filter + tier-ordered farthest-point as default (Gonzalez 1985 farthest-point has provable 2-approximation bound; 89.5% completion is strong empirical floor). Ship one targeted **query-informativeness branch**: classify queries as rich vs bare; widen pool to 250 + drop tier-ordering for bare queries (`BARE_POOL_WIDEN_ENABLED`, default off). Composes with topics 01/03/07 rather than competing. Reject cluster-stratified + curated onboarding. |
| 12 | [Pool-construction ranking methods](12-pool-construction-ranking.md) | Ready for Implementation | **Keep weighted CASE WHEN** as default (RRF/Borda degenerate on binary filter signals; LTR blocked by missing session logs). Ship **normalization fix** (divide by Σ active-filter weights → scores in [0,1], seed boost becomes clean 1.1) as the only real defect. Partially mooted by Topic 01's RRF when raw_query is non-empty; normalized weighted sum is correct only for filter-only path. `pool_scores` is load-bearing **only** for first-10-round tier-grouping. |

## Conventions

Each report follows the structure in `/Users/kms_laptop/.claude/plans/reflective-weaving-seahorse.md` → "Output Conventions":
Status → Question → TL;DR → Context (Current State) → Findings → Options → Recommendation → Open Questions → Proposed Tasks → Sources.
Surveyor-depth (~1500 words, ≥3 external sources).
