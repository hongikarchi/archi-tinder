# Full Codebase Audit — 2026-07-17

**Scope:** entire repo at `da12212` (main == develop, clean tree). Production code; tests skimmed only.
**Method:** ultracode Workflow — 12 dimension finders (Sonnet) → per-finding adversarial verification (Opus, refute-by-default). 114 agents, ~46 min.
**Result:** 102 raw findings → **68 confirmed** / 33 refuted / 1 uncertain. Critical 0 · High 2 · Medium 24 · Low 42.

Dimensions: rules-buildings, back-engine, back-swipe-session, back-llm, back-auth, back-apps-rest, back-security, back-models-migrations, front-contract, front-pages, front-components-design, drift-dead-code.


## HIGH (2)

### `backend/apps/recommendation/views/reports.py:22` — ProjectReportGenerateView + ProjectReportImageView — no throttle, unbounded Gemini image generation
- **dim:** back-security · **rule:** Rate limiting on LLM endpoints
- **evidence:** Both views: permission_classes = [IsAuthenticated], no throttle_classes. ProjectReportImageView calls services.generate_persona_image() (Gemini image gen, highest cost). ProjectReportGenerateView calls services.generate_persona_report(). Zero frequency limit per user.
- **fix:** Add per-user throttles: report text ~3/hr, image generation ~2/hr. Add throttle_classes on both views and register scopes in DEFAULT_THROTTLE_RATES.
- **verifier:** CONFIRMED with code evidence. Both views in backend/apps/recommendation/views/reports.py declare only permission_classes = [IsAuthenticated] (lines 22, 60) and no throttle_classes. Neither guards apply globally: backend/config/settings.py:117-145 defines REST_FRAMEWORK with DEFAULT_THROTTLE_RATES but NO DEFAULT_THROTTLE_CLASSES key (grep for DEFAULT_THROTTLE_CLASSES returns zero hits across backend/), so throttling is strictly opt-in per view — the 'user': '300/min' rate is never enforced unless…

### `backend/apps/recommendation/views/search.py:1` — No rate limiting on ParseQueryView — unbounded Gemini API spend per user
- **dim:** back-llm
- **evidence:** class ParseQueryView(APIView): # no throttle_classes defined; REST_FRAMEWORK in settings.py has no DEFAULT_THROTTLE_CLASSES
- **fix:** Add a UserRateThrottle subclass (e.g. 10 req/min) via throttle_classes on ParseQueryView, ProjectReportGenerateView, and ProjectReportImageView.
- **verifier:** CONFIRMED. Two load-bearing facts verified in code:  1. NO global throttle binding. backend/config/settings.py:117-145 defines REST_FRAMEWORK with DEFAULT_THROTTLE_RATES only — there is NO DEFAULT_THROTTLE_CLASSES key. In DRF, rates without a class binding are inert (the settings comment at line 141 even says the anon/user rates are "applied to views that reference these scopes directly"). So no throttling applies unless a view sets throttle_classes explicitly.  2. None of the three named LLM/im…


## MEDIUM (24)

### `frontend/src/pages/BuildingDetailPage.jsx:195` — BuildingDetailPage: multiple UI strings hardcoded outside t()
- **dim:** front-pages · **rule:** New UI strings go through t() + i18n/locales.js (ko/en both).
- **evidence:** Line 195: 'Building detail' eyebrow label. Lines 289-291: filter chip labels 'All', 'Photos', 'Drawings'. Lines 339,394: section headers 'Photos', 'Drawings'. Line 270: 'View on source' link text. Line 105: 'No atmosphere description is available yet.' fallback. Lines 498-500: 'Description' section header. Lines 515-517: 'Atmosphere' section header. locales.js buildingDetail.* has saved/save/notFound/retry but none of these keys.
- **fix:** Add buildingDetail.eyebrow, buildingDetail.filterAll, buildingDetail.filterPhotos, buildingDetail.filterDrawings, buildingDetail.viewOnSource, buildingDetail.noAtmosphere, buildingDetail.description, buildingDetail.atmosphere keys to locales.js (ko+en). Replace with t() calls.
- **verifier:** Verified against the source. BuildingDetailPage.jsx imports NO i18n hook and uses NO t() call — yet all 8 cited string categories are hardcoded English literals rendered directly in JSX: line 195 'Building detail' eyebrow; chip labels 'All'/'Photos'/'Drawings' (lines 289-291, rendered via {chip.label} at 315); section headers 'Photos' (339) and 'Drawings' (394); 'View on source' link (270); 'No atmosphere description is available yet.' fallback (105, rendered at 529); 'Description' header (497, …

### `frontend/src/pages/DiscoveryPage.jsx:670` — DiscoveryPage: error state, empty state, and swipe hint hardcoded
- **dim:** front-pages · **rule:** New UI strings go through t() + i18n/locales.js (ko/en both).
- **evidence:** Lines 670-692: error state renders "Couldn't load Discovery. Tap to retry." and 'Retry' button hardcoded. Lines 700-723: empty deck shows 'Nothing to show yet' and 'Start Taste Analysis' button hardcoded. Line 796: '← skip · tap card · save →   ·   arrow keys supported' hint hardcoded. Other nav/leave-modal strings correctly use t().
- **fix:** Add discovery.loadError, discovery.retry, discovery.emptyTitle, discovery.startAnalysis, discovery.swipeHint keys to locales.js (ko+en). Replace hardcoded strings with t() calls.
- **verifier:** Confirmed with code evidence. In /Users/kms_laptop/Documents/archi-tinder/make_web/frontend/src/pages/DiscoveryPage.jsx: line 671 renders "Couldn't load Discovery. Tap to retry." and line 689 "Retry" as raw JSX (error branch, reachable via line 662 error && deck.length===0); lines 702 "Nothing to show yet" and 721 "Start Taste Analysis" hardcoded (empty branch, line 694); line 796 swipe hint "← skip · tap card · save → · arrow keys supported" hardcoded (renders on every view). None use t(). Ever…

### `frontend/src/pages/LLMSearchPage.jsx:139` — LLM chat initial AI message and response templates hardcoded in English
- **dim:** front-pages · **rule:** New UI strings go through t() + i18n/locales.js (ko/en both).
- **evidence:** Line 139 (and INITIAL_MESSAGE const used in handleNewConversation line 642): 'Hello! Describe the kind of architecture you're looking for -- country, program, architect, style, year, and so on.' Lines 508-534: 'Found ${results.length} building${...} matching your criteria.', 'No exact matches -- here are some similar buildings you might like.', 'No buildings found. Try describing it differently.' Lines 547,612: 'Something went wrong: ${err.message}. Please try again.' Line 619: 'Untitled Project'. Line 708: 'Update "${initialName}"' header. All bypass t().
- **fix:** Add search.initialMessage, search.foundBuildings, search.noExactMatches, search.noBuildings, search.errorTryAgain, search.untitledProject, search.updateBoard keys to locales.js. Replace literals with t() — use t() with interpolation for count/name params.
- **verifier:** Confirmed verbatim in /Users/kms_laptop/Documents/archi-tinder/make_web/frontend/src/pages/LLMSearchPage.jsx: line 139 and the INITIAL_MESSAGE const at line 642 (used by handleNewConversation at line 650) both hardcode the English initial AI message; lines 508/529 hardcode "Found N buildings matching your criteria."; lines 510/531/598 hardcode the "No exact matches -- ..." fallback default; line 533 hardcodes "No buildings found. Try describing it differently."; lines 547/612 hardcode "Something…

### `frontend/src/pages/SwipePage.jsx:70` — ConfidenceBar stage/count labels hardcoded — bypasses t() (post-i18n convention)
- **dim:** front-pages · **rule:** New UI strings go through t() + i18n/locales.js (ko/en both). fa36cd5 2026-07-14.
- **evidence:** Commit db68d08 (2026-07-17) added ConfidenceBar (lines 70-146). stageLabel fn returns 'Taste found', 'Tuning taste', 'Exploring', 'Calibrating…'; swipeCountLabel returns '${swipeCount} swipes'; heart count returns '${count} ♥'. None in locales.js under swipe.stage.*. Convention established fa36cd5 (2026-07-14).
- **fix:** Add swipe.stage.tastFound/tuning/exploring/calibrating + swipe.swipeCount + swipe.heartCount keys to locales.js (ko+en). Replace literal strings with t() calls inside ConfidenceBar.
- **verifier:** Confirmed by direct code + history inspection. SwipePage.jsx ConfidenceBar (added by commit db68d08 #281, AFTER the i18n convention release fa36cd5 #280) hardcodes English UI strings and never calls useTranslation(): line 85 'Taste found', 88 'Tuning taste', 91 'Exploring', 95 'Calibrating…', 101 `${progress.swipe_count} swipes`, 103 `${...} swipes`, 105 `${...} ♥`. grep confirms 'Taste found'/'Tuning taste'/'Calibrating' exist ONLY in SwipePage.jsx and are absent from frontend/src/i18n/locales.…

### `frontend/src/pages/SwipePage.jsx:504` — Finish button and report-loading label hardcoded (2 instances)
- **dim:** front-pages · **rule:** New UI strings go through t() + i18n/locales.js (ko/en both).
- **evidence:** Line 504 (empty-deck branch) and line 622 (isAt100 branch): `{isResultLoading ? 'Preparing report...' : 'Finish & View Report →'}`. Both instances bypass t(). No matching key in locales.js.
- **fix:** Add swipe.preparingReport + swipe.finishReport keys to locales.js (ko+en). Replace both string literals with t() calls.
- **verifier:** Confirmed with code evidence. SwipePage.jsx line 504 (empty-deck branch) and line 622 (isAt100 top-button branch) both contain the literal `{isResultLoading ? 'Preparing report...' : 'Finish & View Report →'}`, bypassing t(). The file imports useTranslation and uses t() extensively (e.g. line 513 `t('swipe.emptyDeck')` immediately adjacent), so i18n is the established convention. locales.js has a `swipe` namespace (ko line 101 / en line 502) with `emptyDeck` but NO `preparingReport` or `finishRe…

### `frontend/src/pages/UploadWorkPage.jsx:1` — UploadWorkPage almost entirely bypasses i18n despite importing useTranslation
- **dim:** front-pages · **rule:** New UI strings go through t() + i18n/locales.js (ko/en both). fa36cd5 2026-07-14.
- **evidence:** File uses t() for only one call (line 127: uploadWork.error.maxImages). All other UI strings hardcoded: Korean validation messages ('최소 1개의 이미지를 선택해주세요.', '작품 제목을 입력해주세요.', '프로그램 유형을 선택해주세요.', '저작권 확인에 동의해주세요.'), page title '작품 업로드', submitLabel() returning Korean strings, and PROGRAMS constant English labels ('Housing', 'Museum / Cultural', etc.). File introduced 2026-07 (fdf2fe2/6089487) after fa36cd5 i18n convention.
- **fix:** Add uploadWork.* key tree to locales.js (ko+en) covering validation messages, page title, submit labels, and program names. Replace all hardcoded strings with t() calls. The PROGRAMS array labels need both ko+en entries in locales.js.
- **verifier:** Confirmed with code evidence. UploadWorkPage.jsx imports useTranslation (line 77) but the only t() call is line 127 (uploadWork.error.maxImages). All other UI strings are hardcoded: validation messages (lines 168/172/176/180: '최소 1개의 이미지를 선택해주세요.' etc.), page title '작품 업로드' (line 252), submitLabel() Korean returns '변환 중...'/'검토 중...'/'업로드' (lines 229-232), conversion/upload error strings (lines 49/104/221), dropzone copy/labels/placeholders/aria-labels/success message (lines 248-432), and PROGRA…

### `backend/apps/accounts/views/auth.py:415` — GuestPromoteView Branch 2 does not set email_verified_at, creating inconsistency with LinkEmailView
- **dim:** back-auth
- **evidence:** guest_profile.save(update_fields=['is_guest', 'display_name', 'avatar_url']) — 'email_verified_at' missing from update_fields even though google_data['email_verified'] is True and the email is stored. LinkEmailView (line 1029) sets profile.email_verified_at = timezone.now() on the same action.
- **fix:** Add `profile.email_verified_at = timezone.now()` (gated on `google_data.get('email_verified', False)`) before the save call at line 420, and add 'email_verified_at' to the update_fields list. Mirror the LinkEmailView pattern.
- **verifier:** CONFIRMED. GuestPromoteView Branch 2 (backend/apps/accounts/views/auth.py:411-420) stores the verified Google email (safe_email, line 407/412) and flips is_guest=False (line 415), but saves only update_fields=['is_guest','display_name','avatar_url'] (line 420) — email_verified_at is never set in either promote branch. LinkEmailView performs the identical action (verified Google email → verified account) and sets profile.email_verified_at = timezone.now() (auth.py:1029). The model field's own doc…

### `backend/apps/accounts/views/auth.py:458` — GoogleLoginView, KakaoLoginView, NaverLoginView, and TokenRefreshView have no throttle classes
- **dim:** back-auth
- **evidence:** class GoogleLoginView(APIView): ... # no throttle_classes defined (lines 458, 556, 601, 1091). DEFAULT_THROTTLE_CLASSES is not set in REST_FRAMEWORK settings, so these endpoints have zero rate limiting. KakaoLoginView and NaverLoginView make outbound HTTP calls on every unauthenticated request.
- **fix:** Add throttle_classes = [AnonRateThrottle] (or a dedicated scoped throttle) to GoogleLoginView, KakaoLoginView, NaverLoginView, and TokenRefreshView. Add corresponding scope entries in DEFAULT_THROTTLE_RATES. The existing GuestLoginThrottle at 3/min and PasswordLoginThrottle at 10/min are the model to follow.
- **verifier:** CONFIRMED with code evidence. GoogleLoginView (auth.py:458), KakaoLoginView (:556), NaverLoginView (:601), TokenRefreshView (:1091) all have permission_classes=[AllowAny], authentication_classes=[], and NO throttle_classes. REST_FRAMEWORK (settings.py:117-145) defines DEFAULT_THROTTLE_RATES but no DEFAULT_THROTTLE_CLASSES — `grep DEFAULT_THROTTLE_CLASSES` over the entire backend returns empty. In DRF, rates without a throttle class (default or per-view) produce ZERO throttling, so these four pub…

### `backend/apps/recommendation/services/hydration.py:188` — Missing is_publishable gate in _batch_fetch_embeddings
- **dim:** rules-buildings · **rule:** Every canonical_v2_buildings query must gate on is_publishable = true
- **evidence:** SELECT canonical_bld_id, embedding::text FROM canonical_v2_buildings WHERE canonical_bld_id IN ({placeholders}) — no AND is_publishable = true
- **fix:** Add AND is_publishable = true to the WHERE clause. The inline comment justification ('already-liked = already-publishable when served') is operationally reasonable but violates the stated hard rule that EVERY canonical_v2_buildings query must gate on is_publishable = true. A building could be unpublished after a user likes it; the gate also protects against future callers reusing this function with non-liked IDs.
- **verifier:** CONFIRMED. backend/apps/recommendation/services/hydration.py:188-194 runs `SELECT canonical_bld_id, embedding::text FROM canonical_v2_buildings WHERE canonical_bld_id IN ({placeholders})` with no `AND is_publishable = true`. The inline comment at 172-173 ("No is_publishable gate needed here because these ids are already-liked buildings — they were publishable when served") is the author's self-justification, NOT a sanctioned carve-out — and the codebase's own convention explicitly REJECTS that e…

### `backend/apps/recommendation/services/session_service.py:562` — Lost-update race on exposed_ids in get_session_state: concurrent swipe can overwrite session state
- **dim:** back-swipe-session
- **evidence:** exposed_ids = exposed_ids + [next_bid] / session.exposed_ids = exposed_ids / session.save(update_fields=['exposed_ids']) — no select_for_update(); a concurrent swipe_service call holding select_for_update on the same session row and saving exposed_ids will overwrite this append, or vice versa; one write is silently lost
- **fix:** Either (a) acquire select_for_update() on the session before entering this branch, inside a transaction.atomic() block; or (b) use F-expression / ArrayAppend to make the append atomic at DB level rather than read-modify-write in Python.
- **verifier:** Confirmed with code evidence. session_service.py:562-565 does a Python-side read-modify-write: `exposed_ids = exposed_ids + [next_bid]; session.exposed_ids = exposed_ids; session.save(update_fields=['exposed_ids'])`. The view that supplies `session` (views/sessions.py:62) fetches it with a bare `AnalysisSession.objects.filter(...).first()` — no select_for_update(), no transaction.atomic(). This is the only persisting exposed_ids write in get_session_state (the other mutation at line 607 is in-me…

### `backend/apps/recommendation/views/projects.py:117` — ProjectListCreateView guest board-limit also counts is_temp=True boards — diverges from discovery limit
- **dim:** back-apps-rest
- **evidence:** `Project.objects.filter(user=profile).count() >= 3` at line 117 blocks new-board creation when 3 temp sessions exist, even though discovery promotes (DiscoveryPromoteView:407) use `is_temp=False`. A guest with 3 in-progress temp sessions is blocked here but not at discovery promote.
- **fix:** Change to `Project.objects.filter(user=profile, is_temp=False).count() >= 3` to match the discovery-view definition of the guest limit.
- **verifier:** CONFIRMED REAL. backend/apps/recommendation/views/projects.py:117 reads `if profile.is_guest and Project.objects.filter(user=profile).count() >= 3:` — verified verbatim, with NO is_temp filter. Three sibling guest-cap sites all filter is_temp=False and document the intent: discovery.py:270 ("Count only permanent (is_temp=False) boards — temp boards are in-progress taste sessions... must not trigger the gate early"), discovery.py:407 (DiscoveryPromoteView, matches auditor's cited line), and proje…

### `backend/apps/recommendation/views/reports.py:1` — No rate limiting on ProjectReportGenerateView and ProjectReportImageView — unbounded Gemini spend
- **dim:** back-llm
- **evidence:** class ProjectReportGenerateView(APIView): # no throttle_classes; class ProjectReportImageView(APIView): # no throttle_classes
- **fix:** Same fix as ParseQueryView: add UserRateThrottle subclass. Report generation is more expensive (buildings DB query + Gemini text + Gemini image gen) so a lower rate (e.g. 3/min) is appropriate.
- **verifier:** Core claim VERIFIED with code evidence. ProjectReportGenerateView (backend/apps/recommendation/views/reports.py:21) and ProjectReportImageView (reports.py:59) have no throttle_classes. Settings has NO DEFAULT_THROTTLE_CLASSES (backend/config/settings.py:116-142 defines only DEFAULT_THROTTLE_RATES scopes) and no rate-limiting middleware (settings.py:40-50), so DRF applies zero throttling to these views. Both hit real Gemini APIs: generate_persona_report (generation.py:256 generate_content_with_fa…

### `backend/apps/recommendation/views/search.py:99` — ParseQueryView — no throttle, unbounded Gemini LLM calls
- **dim:** back-security · **rule:** Rate limiting on auth + LLM endpoints
- **evidence:** permission_classes = [IsAuthenticated] with no throttle_classes. Each POST triggers services.parse_query() → Gemini API call. Authenticated users can spam at will, driving up LLM costs.
- **fix:** Add throttle_classes = [ParseQueryThrottle] with scope 'parse_query' at ~5-10/min per user. Register scope in DEFAULT_THROTTLE_RATES.
- **verifier:** Confirmed real. backend/apps/recommendation/views/search.py:99-100 declares `class ParseQueryView(APIView)` with `permission_classes = [IsAuthenticated]` and NO `throttle_classes`. Branch B (free-text turn, no priority_axis) calls `services.parse_query(...)` at line 274, which invokes a real Gemini call (`_svc.generate_content_with_fallback`, parse_query.py:480/497). No request-rate guard exists elsewhere: config/settings.py:117-145 REST_FRAMEWORK defines `DEFAULT_THROTTLE_RATES` scopes but has …

### `frontend/src/api/core.js:35` — JWT access and refresh tokens stored in localStorage — vulnerable to XSS token theft
- **dim:** front-components-design · **rule:** Frontend security: token storage
- **evidence:** localStorage.setItem('archithon_access',  access)  // line 35
localStorage.setItem('archithon_refresh', refresh)  // line 36
- **fix:** Prefer httpOnly cookies for JWTs to eliminate XSS access to tokens. If localStorage must be kept (SPA convenience), document the decision as an accepted risk and ensure a strict CSP header is deployed to reduce XSS surface. A short access-token lifetime (currently 1 hr) partially mitigates but refresh token exposure in localStorage is high-risk.
- **verifier:** CONFIRMED. frontend/src/api/core.js:33-41 stores JWT access + refresh tokens in localStorage exactly as quoted (setItem 'archithon_access'/'archithon_refresh' on lines 35-36). Consistent across the codebase: core.js:70/80-82 (refresh path), App.jsx:937, DebugOverlay.jsx:37 all use the same localStorage keys — there is NO httpOnly cookie mechanism anywhere. No guard exists: backend prod security (backend/config/settings.py:433-447) sets HSTS + SECURE_BROWSER_XSS_FILTER + nosniff + X_FRAME_OPTIONS…

### `frontend/src/api/projects.js:64` — updateProject silently swallows 403 verify_required path — VerifyRequiredError never thrown for PATCH
- **dim:** front-contract
- **evidence:** export async function updateProject(projectId, fields) { try { return await callApi('PATCH', ...) } catch (err) { console.error(...); throw err } } — re-throws raw err without checking 403 verify_required; createProject at line 26 has the guard; PATCH /projects/{pk}/ is equally capable of returning 403 on the is_temp:false promotion path for guest users
- **fix:** Add the same 403 check as createProject: if (err?.status === 403 && err?.data?.detail === 'verify_required') dispatch archithon:verify-required and throw new VerifyRequiredError(reason), so all callers of updateProject automatically get correct behavior.
- **verifier:** CONFIRMED. Code matches claim: updateProject (frontend/src/api/projects.js:64-71) catches and re-throws the raw error with NO 403 verify_required guard, unlike its sibling createProject (same file, lines 30-37) and the identical guards in liked.js:17, sessions.js:40, discovery.js:33.  Reachable path proven end-to-end: (1) Backend PATCH /projects/{pk}/ DOES return 403 verify_required on the is_temp:false guest-promotion path — backend/apps/recommendation/views/projects.py:206-218 returns {'detail…

### `frontend/src/components/SaveToBoardModal.jsx:161` — SaveToBoardModal uses fontWeight 800 in two places, hardcodes #ec4899 gradient, uses skeleton-shimmer, and hardcodes error color #fca5a5
- **dim:** front-components-design · **rule:** DESIGN.md §2.5; §8.8; §1.2; §1.3
- **evidence:** fontWeight: 800  // lines 161, 273
background: 'linear-gradient(135deg,#ec4899,#f43f5e)'  // lines 199, 248
className="skeleton-shimmer"  // line 268
color: '#fca5a5'  // lines 182, 260
- **fix:** Lower fontWeight to 700. Replace gradient with var(--accent-1)/var(--accent-2). Replace skeleton-shimmer with lp-skel. Replace '#fca5a5' with 'var(--color-destructive)'.
- **verifier:** All four sub-claims verified against real code in /Users/kms_laptop/Documents/archi-tinder/make_web/frontend/src/components/SaveToBoardModal.jsx. (1) fontWeight:800 confirmed at line 161 (`<h3 style={{ fontSize: 17, margin: 0, fontWeight: 800 }}>`) and line 273 (`fontSize: 11, fontWeight: 800,`) — violates DESIGN.md §2.5 'Maximum 700. 800/900 forbidden.' (2) Hardcoded gradient `linear-gradient(135deg,#ec4899,#f43f5e)` confirmed at lines 199 and 248 — these are old-design pink/rose values, not th…

### `frontend/src/components/SurpriseBoardModal.jsx:15` — SurpriseBoardModal component is never imported — dead file
- **dim:** drift-dead-code
- **evidence:** grep -rn 'import.*SurpriseBoardModal' frontend/src/ returns zero results outside the file itself. App.jsx line 83 only mentions it in a comment ('Tracks whether a SurpriseBoardModal board-create was interrupted'). The component is not rendered anywhere.
- **fix:** Delete frontend/src/components/SurpriseBoardModal.jsx and its associated dead state in App.jsx (surprisePending, the event-listener useEffect at lines 168-173, and the surprisePending branch at line 1298-1301).
- **verifier:** CONFIRMED dead code / drift. frontend/src/components/SurpriseBoardModal.jsx (387 lines, export at line 15) is never imported or rendered in the current tree. grep -rn 'SurpriseBoardModal' finds only: (a) the component's own file, and (b) App.jsx:83, which is a bare comment. No import statement, no <SurpriseBoardModal/> JSX usage anywhere. Git history proves it's DRIFT: the import `import SurpriseBoardModal from '../components/SurpriseBoardModal.jsx'` formerly lived in frontend/src/pages/Discover…

### `frontend/src/components/TabBar.jsx:81` — TabBar active color hardcoded as #ec4899 instead of var(--accent-1)
- **dim:** front-components-design · **rule:** DESIGN.md §1.2: accent colors are themed CSS variables
- **evidence:** color: active ? '#ec4899' : 'var(--color-nav-inactive)',  // line 81
background: active ? 'rgba(236,72,153,0.12)' : 'transparent',  // line 89
- **fix:** Replace '#ec4899' with 'var(--accent-1)' and 'rgba(236,72,153,0.12)' with 'color-mix(in srgb, var(--accent-1) 12%, transparent)'. The TabBar active indicator is theme-aware per design (accent-1 changes across GitHub Light/Dark/Ayu/SynthWave), but the hardcoded pink stays pink in every theme.
- **verifier:** CODE VERIFIED. TabBar.jsx:81 is exactly `color: active ? '#ec4899' : 'var(--color-nav-inactive)'` and :89 is `background: active ? 'rgba(236,72,153,0.12)' : 'transparent'`. The claim is accurate.  Refutation checks all fail to overturn it: 1. Does what claim says: yes, hardcoded pink for the active state. 2. Not guarded elsewhere: inline styles have near-highest CSS specificity, so no theme rule can override them. The SAME ternary's inactive branch uses the themed `var(--color-nav-inactive)`, pr…

### `frontend/src/components/profile/BoardCard.jsx:106` — BoardCard selected ring and multiple hover handlers hardcode #ec4899; 12 onMouseEnter/Leave handlers
- **dim:** front-components-design · **rule:** DESIGN.md §4: hover via CSS :hover only; §1.2: accent colors are themed
- **evidence:** boxShadow: isSelected ? '0 0 0 3px #ec4899' : 'none',  // line 106
background: isSelected ? '#ec4899' : 'rgba(0,0,0,0.4)',  // line 184
onMouseEnter/Leave used 12 times (lines 108-109, 222-223, 299-300, 446-447, 465-466, 485-489)
- **fix:** Replace '#ec4899' with 'var(--accent-1)'. Extract hover interactions (translateY lift, button background changes) to BoardCard.module.css using :hover. The state-driven lift (isHovered) is needed only because JS hover handles transitions that CSS :hover could own.
- **verifier:** Verified against source. BoardCard.jsx:106 (`boxShadow: isSelected ? '0 0 0 3px #ec4899' : 'none'`) and :184 (`background: isSelected ? '#ec4899' : 'rgba(0,0,0,0.4)'`) both hardcode #ec4899. grep confirms exactly 12 onMouseEnter/onMouseLeave handlers at the cited lines. #ec4899 is NOT any theme token — tokens.css defines --accent-1 as #0969DA / #2f81f7 / #FA8D3E / #FF7EDB across the 4 themes (grep for ec4899 in tokens.css and all *.css returns no match), so the selected-board ring/circle renders…

### `frontend/src/pages/ArchitectProfilePage.jsx:155` — Same liked_ids/saved_ids object-vs-string bug as FirmProfilePage — savedIds Set contains objects not strings
- **dim:** front-contract
- **evidence:** for (const id of (p.liked_ids || [])) ids.add(id) / for (const id of (p.saved_ids || [])) ids.add(id) — identical pattern to FirmProfilePage.jsx; backend ProjectSerializer sends object arrays; extractLikedIds/extractSavedIds not used
- **fix:** Same fix as FirmProfilePage: use extractLikedIds(p.liked_ids) and extractSavedIds(p.saved_ids) from appHelpers.js.
- **verifier:** CONFIRMED. ArchitectProfilePage.jsx:155-156 iterates `p.liked_ids`/`p.saved_ids` and does `ids.add(id)` where `id` is actually an object, not a string. Backend shape is objects: appHelpers.js:39-56 documents `liked_ids: list[{id, intensity}]` and `saved_ids: list[{id, saved_at}]`; backend tests prove it (test_sessions.py:1052 `liked_ids[0] == {'id':'B00001','intensity':1.0}`; test_migrations_legacy_entries.py:79 reads `e['id']` on saved_ids). The Set therefore holds objects. It is consumed at li…

### `frontend/src/pages/FirmProfilePage.jsx:30` — liked_ids/saved_ids iterated as plain strings but backend returns object arrays — savedIds.has(buildingId) is always false
- **dim:** front-contract
- **evidence:** for (const id of (p.liked_ids || [])) ids.add(id) / for (const id of (p.saved_ids || [])) ids.add(id) — ProjectSerializer returns liked_ids as list[{id, intensity}] and saved_ids as list[{id, saved_at}]; the Set ends up containing objects, so any downstream ids.has(stringBldId) returns false; extractLikedIds()/extractSavedIds() helpers in appHelpers.js normalize both shapes but are not used here
- **fix:** Import extractLikedIds and extractSavedIds from appHelpers.js and replace the loop body: for (const id of extractLikedIds(p.liked_ids)) ids.add(id) / for (const id of extractSavedIds(p.saved_ids)) ids.add(id).
- **verifier:** Confirmed with code evidence. FirmProfilePage.jsx:30-31 does `for (const id of (p.liked_ids || [])) ids.add(id)` / `for (const id of (p.saved_ids || [])) ids.add(id)`, adding raw entries to a Set. The backend serializes these as object arrays: `ProjectListSerializer` (serializers.py:200-217) excludes only analysis_report/conversation_history/axis_scores, so liked_ids/saved_ids ARE returned; they are plain model JSONFields (NOT SerializerMethodFields — only latest_session_id/latest_session_meta a…

### `frontend/src/pages/settings/AccountScreen.jsx:104` — Handle validation regex blocks valid Hangul IDs and 2-char IDs, mismatches backend rules
- **dim:** front-contract
- **evidence:** if (trimmed && !/^[a-z0-9_]{3,30}$/.test(trimmed)) { — allows only lowercase ASCII a-z, digits, underscore, 3-30 chars; backend validate_handle_value() accepts Hangul + ASCII letters (upper+lower) + digits + underscore, 2-20 chars (NFC-normalized)
- **fix:** Replace the regex with the same pattern used in loginFlow.js isIdFormatValid(): /^[가-힣A-Za-z0-9_]{2,20}$/ (after NFC normalization), and reduce the max from 30 to 20 to match backend enforcement.
- **verifier:** VERIFIED REAL. AccountScreen.jsx:104 is `if (trimmed && !/^[a-z0-9_]{3,30}$/.test(trimmed))` — the exact stale ASCII-only pattern (lowercase a-z, digits, underscore, 3-30 chars). Backend `validate_handle_value()` (backend/apps/accounts/serializers.py:127-180, regex `_HANDLE_RE` at :122-124 = `^[가-힣ᄀ-ᇿꥠ-꥿ힰ-퟿a-zA-Z0-9_]+$`, length 2-20 after NFC) accepts Hangul + upper/lowercase ASCII + digits + underscore, 2-20 chars — its own docstring (:143-144) says it was "extended from ASCII-only (^[a-z0-9_]…

### `web-testing/runner/runner.py:507` — Stale UI selectors: 'Create new folder' and '/new' route no longer exist in frontend
- **dim:** drift-dead-code
- **evidence:** page.get_by_text('Create new folder').wait_for(...) and page.wait_for_url('**/new', timeout=5000) — the current App.jsx has no '/new' route and no 'Create new folder' button; the flow now goes directly to /discovery on login
- **fix:** Remove Steps 2 and 3 (home + project-setup) and update runner to start from /search or /discovery. The LoginPage + DiscoveryPage are now the entry points. The docstring at line 6 also needs updating ('SetupPage (/) -> ProjectSetupPage (/new)' is stale).
- **verifier:** Confirmed real via code evidence. runner.py:507 waits for get_by_text('Create new folder') (10s timeout) and :533 waits for URL '**/new'. Grep of frontend/src for 'Create new folder', '/new', 'ProjectSetupPage', 'SetupPage' returns ZERO hits. frontend/src/App.jsx routing table (lines 1077-1128) has no '/new' route; the index route (line 1087) redirects to /discovery, and the catch-all '*' (line 1127) redirects unknown paths to '/'→/discovery. So clicking a non-existent button and awaiting /new b…

### `web-testing/runner/runner.py:859` — Stale text selectors 'All done!', 'View Image Board', 'Generate Persona Report', and 'Persona Type' label do not exist in current frontend
- **dim:** drift-dead-code
- **evidence:** Lines 859 (text=All done!), 864/1118 (text=View Image Board), 1177 (text=Generate Persona Report), 1189 (text=Persona Type) — grep across entire frontend/src finds none of these strings in JSX or locales. PersonaReport.jsx renders {report.persona_type} value directly with no 'Persona Type' label. Reports are auto-generated on session end.
- **fix:** Remove or replace these steps: detect session completion via swipe API's is_analysis_completed flag rather than DOM text; detect persona report presence by checking the /result/:sessionId page renders PersonaReport content (e.g., an <h1> with persona type value); remove 'Generate Persona Report' step entirely as it is automatic.
- **verifier:** Verified real. The cited runner.py lines exist exactly as described (859 text=All done!, 864/1118 text=View Image Board, 1177 text=Generate Persona Report, 1189 text=Persona Type). Grep across the ENTIRE repo (frontend/src, backend, i18n locales.js, case-insensitive) confirms all four strings exist ONLY in web-testing/runner/runner.py and nowhere in the application. Current frontend detects completion via the is_analysis_completed API flag (App.jsx:317,622,709; SwipePage.jsx:430) backed by sessi…


## LOW (42)

### `frontend/src/pages/ArchitectProfilePage.jsx:151` — listProjects fetch in useEffect has no cancellation — setState on unmounted component
- **dim:** front-pages · **rule:** Unmounted setState leak — standard React cleanup pattern.
- **evidence:** Lines 151-160: `useEffect(() => { listProjects().then(resp => { ... setSavedIds(ids) }).catch(() => {}) }, [])`. No cancelled ref, no cleanup return. If component unmounts before the promise resolves (e.g. user navigates away quickly), setSavedIds fires on an unmounted component. React 18 no-ops this but logs a warning in dev; consistent with the pattern other pages (DiscoveryPage fetchPage, LoginPage roles fetch) use correctly.
- **fix:** Add `let cancelled = false` before the promise chain, guard `setSavedIds` with `if (!cancelled)`, return `() => { cancelled = true }` from the effect.
- **verifier:** Verified verbatim at ArchitectProfilePage.jsx:151-160: `useEffect(() => { listProjects().then(resp => {...setSavedIds(ids)}).catch(() => {}) }, [])` — no `cancelled` flag, no cleanup return, `setSavedIds` (line 158) fires unconditionally in the resolved promise. The claim's supporting evidence is confirmed: the `let cancelled=false / if(!cancelled) / return () => {cancelled=true}` pattern is the established house convention, present in LoginPage.jsx:108-110, BuildingDetailPage.jsx:53/73, FirmPro…

### `frontend/src/pages/ArchitectProfilePage.jsx:267` — ArchitectProfilePage: multiple UI strings hardcoded outside t()
- **dim:** front-pages · **rule:** New UI strings go through t() + i18n/locales.js (ko/en both).
- **evidence:** Line 267: <span>Office</span> page title. Line 466-468: 'Buildings' section header. Lines 474-480: 'Followers' stat label. Line 506: {isFollowing ? 'Following' : 'Follow'} button text (aria-label uses t() but visible text doesn't). Lines 523-524: 'Website' button. Lines 540-541: 'Contact' button. Line 580: 'Buildings' section label. Lines 612,630: 'Built · {n}' and 'Unbuilt · {n}' tab labels. Line 643-644: 'No projects yet' empty state. locales.js has architect.follow/unfollow/website/email but not office/buildings/followers/following/built/unbuilt/noProjects.
- **fix:** Add architect.office, architect.buildings, architect.followers, architect.following, architect.contact, architect.built, architect.unbuilt, architect.noProjects keys to locales.js (ko+en). Replace hardcoded strings with t() calls.
- **verifier:** Confirmed against primary source. Every cited hardcoded English string exists at the stated lines in /Users/kms_laptop/Documents/archi-tinder/make_web/frontend/src/pages/ArchitectProfilePage.jsx: line 267 <span>Office</span>, 467 'Buildings' (stat-count label), 480 'Followers', 506 {isFollowing ? 'Following' : 'Follow'} (aria-label at 504 uses t() but visible text doesn't), 523 'Website', 540 'Contact', 580 'Buildings' (the actual section header — auditor mislabeled 467 as the section header; bo…

### `frontend/src/pages/BoardDetailPage.jsx:13` — MOCK_BOARD constant defined but never used in render path — dead code
- **dim:** front-pages · **rule:** Dead code — not a hard project rule violation but confusing and should be cleaned.
- **evidence:** Lines 13-141: large `const MOCK_BOARD = { ... }` object with TODO comment. The actual data source is `useBoard(boardId)` (line 149). MOCK_BOARD is never referenced in render or hooks after definition. Dead code adds ~130 lines and may mislead future editors into thinking it is the data source.
- **fix:** Delete the MOCK_BOARD constant block. If needed for local dev/testing, move to a separate mock fixture file.
- **verifier:** Confirmed dead code. `MOCK_BOARD` is a module-level `const` defined at BoardDetailPage.jsx:19-141 (~123 lines, 12 mock buildings). A full-repo grep (excluding node_modules) finds only three hits: the definition (line 19), a TODO comment above it (line 13: "Replace MOCK_BOARD with API call to GET /api/v1/boards/..."), and an unrelated code comment in boardDetail/BuildingTile.jsx:17. It is never imported, exported, or read in any hook, JSX, or logic. The real data source is `useBoard(boardId)` at …

### `frontend/src/pages/SwipePage.jsx:588` — Filter-relaxed notification and swipe hint hardcoded
- **dim:** front-pages · **rule:** New UI strings go through t() + i18n/locales.js (ko/en both).
- **evidence:** Line 588: 'Filters were relaxed to find more buildings' inline notification. Line 689: '← skip · tap card · save →' swipe gesture hint. Both bypass t().
- **fix:** Add swipe.filtersRelaxed and swipe.gestureHint to locales.js (ko+en). Replace with t() calls.
- **verifier:** Verified in SwipePage.jsx: line 588 'Filters were relaxed to find more buildings' and line 689 '← skip · tap card · save →' are both hardcoded English JSX literals that bypass t(). The i18n system is real and actively used in this exact file (useTranslation imported line 10; const { t } = useTranslation() in the main SwipePage component at line 340; t() used at lines 17/151/248/340). The swipe namespace already exists in locales.js for both ko (line 101) and en (line 502), but no filtersRelaxed/…

### `frontend/src/pages/UserProfilePage.jsx:423` — UserProfilePage: loading/error states, tab labels, and selection-mode UI hardcoded
- **dim:** front-pages · **rule:** New UI strings go through t() + i18n/locales.js (ko/en both).
- **evidence:** Line 423: 'Loading profile...' loading state. Line 436: 'Profile not found.' error fallback. Lines 505,525,546,567: tab labels 'Boards', 'Studios', 'Liked', 'Created'. Line 622: 'Cancel' in select-mode header. Line 629: '{selectedBoards.size} selected' template. Line 649: {allSelected ? 'Deselect all' : 'Select all'}. Line 663: 'Curated Boards' section header. Note: line 234 has `const t = setTimeout(...)` inside useEffect which shadows the outer t from useTranslation — not a bug since t() isn't called there, but naming collision.
- **fix:** Add profile.loading, profile.notFound, profile.tabBoards, profile.tabStudios, profile.tabLiked, profile.tabCreated, profile.cancel, profile.selectedCount, profile.deselectAll, profile.selectAll, profile.curatedBoards keys to locales.js (ko+en). Rename the timeout variable inside useEffect to avoid shadowing.
- **verifier:** Verified against UserProfilePage.jsx. All cited hardcoded strings exist exactly as claimed: line 423 'Loading profile...', line 436 'Profile not found.' fallback, lines 505/525/546/567 tab labels Boards/Studios/Liked/Created, line 622 'Cancel', line 629 '{selectedBoards.size} selected', line 649 'Deselect all'/'Select all', line 663 'Curated Boards'. The i18n convention is genuinely LIVE in this file: line 3 imports useTranslation, line 33 does `const { t } = useTranslation()`, and the file alre…

### `backend/apps/accounts/views/auth.py:463` — GoogleLoginView accepts direct access_token (implicit flow) in addition to auth-code, allowing a weaker OAuth flow
- **dim:** back-auth
- **evidence:** access_token = request.data.get('access_token') ... if code and not access_token: [auth-code path] ... [else: direct access_token path]. If both are provided, access_token takes precedence. A caller can bypass auth-code exchange and supply an implicit-flow token obtained from any source. Google deprecated implicit flow for SPAs in 2019.
- **fix:** Remove the direct access_token path and require the auth-code flow exclusively. If backward compatibility is needed, add a validation step to confirm the access_token was obtained via server-side exchange (e.g. require a matching nonce or enforce origin check). At minimum, log a deprecation warning and document the intended removal.
- **verifier:** CONFIRMED at backend/apps/accounts/views/auth.py:463-467. Line 463 `access_token = request.data.get('access_token')`; line 467 `if code and not access_token:` runs the server-side auth-code exchange ONLY when a code is present and no access_token was supplied. Any caller may instead POST a raw access_token and skip the exchange entirely — the token is used directly at line 510-514 against /oauth2/v3/userinfo. The endpoint is `permission_classes=[AllowAny]`, `authentication_classes=[]` (lines 459…

### `backend/apps/accounts/views/auth.py:556` — KakaoLoginView and NaverLoginView do not call record_device_and_maybe_notify, so new-device security notifications are not sent for Kakao/Naver logins
- **dim:** back-auth
- **evidence:** GoogleLoginView (line 547) and PasswordLoginView (line 846) call record_device_and_maybe_notify(). KakaoLoginView.post() (lines 560-596) and NaverLoginView.post() (lines 605-640) do not. New-device logins via these providers are silently accepted with no notification.
- **fix:** Add a try/except-wrapped call to record_device_and_maybe_notify(profile, request.META.get('HTTP_USER_AGENT', '')) immediately before the return Response(_make_token_response(profile)) in both KakaoLoginView and NaverLoginView, mirroring the GoogleLoginView pattern.
- **verifier:** Confirmed accurate with code evidence. record_device_and_maybe_notify() is called only in GoogleLoginView (backend/apps/accounts/views/auth.py:547) and PasswordLoginView (auth.py:846); KakaoLoginView.post (ends auth.py:596) and NaverLoginView.post (ends auth.py:640) both return _make_token_response(profile) directly without it. (1) Code matches the claim exactly. (2) NOT guarded elsewhere: grep for record_device_and_maybe_notify across backend/ returns exactly two call sites; the only accounts s…

### `backend/apps/accounts/views/auth.py:904` — SetPasswordView password save and token blacklist are not atomic; a DB error during blacklisting leaves old tokens active after password change
- **dim:** back-auth
- **evidence:** user.set_password(password); user.save(update_fields=['password'])  # line 904-905 — then — for outstanding in OutstandingToken.objects.filter(user=user): BlacklistedToken.objects.get_or_create(token=outstanding)  # line 916. The save and blacklist are sequential without a wrapping transaction.atomic(). If any blacklist DB op fails, the password is changed but old tokens remain valid.
- **fix:** Wrap lines 904-917 in a single transaction.atomic() so the password save and token blacklist succeed or fail together. Issue the fresh token response outside the transaction block.
- **verifier:** Code matches the claim (auth.py:904-917): user.set_password() + user.save(update_fields=['password']) run, then a separate `for outstanding in OutstandingToken.objects.filter(user=user): BlacklistedToken.objects.get_or_create(token=outstanding)` loop, with NO wrapping transaction.atomic(). Confirmed non-atomic: settings.py:73-87 sets no ATOMIC_REQUESTS on the 'default' DB (autocommit mode), and the same file DOES use transaction.atomic() at 273/342/751/998 for other multi-write ops. The blacklis…

### `backend/apps/profiles/models.py:72` — OfficeProjectLink.building_id has two duplicate standalone indexes
- **dim:** back-models-migrations
- **evidence:** `building_id = models.TextField(db_index=True)` plus `models.Index(fields=['building_id'])` in Meta.indexes (migration 0001 confirms both: `db_index=True` on the field AND `AddIndex(fields=['building_id'], name='profiles_of_buildin_cd1dab_idx')`). Django creates both as separate B-tree indexes on the same column.
- **fix:** Remove either `db_index=True` from the field declaration or the explicit `Index(['building_id'])` from Meta.indexes. The `unique_together(office, building_id)` composite already covers paired lookups; the single-column index is only needed for reverse lookups (all offices for a building), so keep the explicit `Meta.indexes` entry and drop `db_index=True` from the field.
- **verifier:** Confirmed with code evidence. models.py:72 declares `building_id = models.TextField(db_index=True)` (field-level single-column index) AND models.py:81-82 declares `Meta.indexes = [models.Index(fields=['building_id']), ...]` (a second single-column index). Migration 0001_initial.py corroborates both are actually created as separate B-tree indexes: line 41 `('building_id', models.TextField(db_index=True))` and line 58 `AddIndex(index=models.Index(fields=['building_id'], name='profiles_of_buildin_c…

### `backend/apps/recommendation/engine.py:2017` — compute_dpp_topk hardcodes round_num=0, defeating recency weighting
- **dim:** back-engine
- **evidence:** centroids, _ = compute_taste_centroids(like_vectors, round_num=0) — recency weight = exp(-gamma * max(0, 0 - entry_round)) = 1.0 for all entries, so every liked building gets equal weight regardless of when it was liked
- **fix:** Pass the actual round_num from the call site: compute_taste_centroids(like_vectors, round_num=round_num). Add round_num parameter to compute_dpp_topk signature.
- **verifier:** Claim mechanic is factually correct. engine.py:2017 hardcodes compute_taste_centroids(like_vectors, round_num=0). That flows to engine_convergence.py:26: weight = math.exp(-gamma * max(0, round_num - entry_round)). Likes store 'round': session.current_round at swipe time (swipe_service.py:800), so entry_round >= 0. With round_num=0, max(0, 0 - entry_round) = 0 for every entry, giving weight = 1.0 uniformly — recency weighting is genuinely defeated in this branch, exactly as claimed. Not compensa…

### `backend/apps/recommendation/models.py:132` — SwipeEvent.idempotency_key standalone db_index is redundant — unique_together covers every production query
- **dim:** back-models-migrations
- **evidence:** `idempotency_key = models.CharField(max_length=100, db_index=True)` and `unique_together = [('session', 'idempotency_key')]`. The only production idempotency query is `SwipeEvent.objects.filter(idempotency_key=idempotency_key, session=session)` (swipe.py:276-278), which the composite unique index covers optimally. The standalone index on `idempotency_key` alone is never the better choice and wastes write overhead on every swipe insert.
- **fix:** Remove `db_index=True` from the `idempotency_key` field declaration and add a migration `AlterField` to drop it. The `unique_together` index alone is sufficient.
- **verifier:** Verified the code matches: models.py:132 `idempotency_key = models.CharField(max_length=100, db_index=True)` plus models.py:136 `unique_together = [('session', 'idempotency_key')]`. Grep across ALL non-test backend code confirms exactly ONE production read query on idempotency_key — views/swipe.py:276-278 `SwipeEvent.objects.filter(idempotency_key=idempotency_key, session=session).exists()` — which always includes the session equality predicate. The only other reference is the insert (swipe_serv…

### `backend/apps/recommendation/services/_gemini.py:1` — _get_client() singleton check-then-set has no lock — thread-unsafe initialization
- **dim:** back-llm
- **evidence:** if _client is None:\n    _client = genai.Client(api_key=settings.GEMINI_API_KEY)  # no threading.Lock; two concurrent threads can both see None and create duplicate clients
- **fix:** Use threading.Lock around the check-then-set, or initialize _client eagerly at module load time (after settings are available via AppConfig.ready).
- **verifier:** CONFIRMED. Code at backend/apps/recommendation/services/_gemini.py:70-74 is exactly as claimed: `def _get_client(): global _client; if _client is None: _client = genai.Client(api_key=settings.GEMINI_API_KEY); return _client` — an unlocked check-then-set on module-global `_client` (init'd None at line 25).  Reachability (this is genuinely a multi-thread process, not falsified): backend/railway.toml:15 runs `gunicorn ... --workers 3` (no --threads, so sync workers) BUT the app explicitly spawns pe…

### `backend/apps/recommendation/services/session_service.py:539` — TOCTOU race on action_card_shown: two concurrent GET /session-state/ can both emit the action card
- **dim:** back-swipe-session
- **evidence:** if not session.action_card_shown: / next_card = _build_action_card() / session.action_card_shown = True / session.save(update_fields=['action_card_shown']) — session loaded without select_for_update(); two concurrent readers both see action_card_shown=False and both set it True, resulting in the convergence action card shown twice
- **fix:** Load session with select_for_update() in SessionStateView.get (or inside get_session_state for the converged branch) so the read-modify-write on action_card_shown is atomic. Alternatively use update_or_create / F-expression approach: AnalysisSession.objects.filter(pk=session.pk, action_card_shown=False).update(action_card_shown=True) and only emit the card if rowcount==1.
- **verifier:** Code-level claim CONFIRMED. session_service.py:539-543 does an unguarded read-modify-write on session.action_card_shown (read False -> _build_action_card() -> set True -> save(update_fields=['action_card_shown'])). SessionStateView.get (views/sessions.py:62) loads the session with `AnalysisSession.objects.filter(session_id=..., user=profile).first()` — no select_for_update, and get_session_state has no @transaction.atomic. The swipe path DOES use select_for_update (swipe_service.py:621/729) but …

### `backend/apps/recommendation/services/swipe_service.py:624` — handle_swipe_extend noop branch can_continue always evaluates to False
- **dim:** back-swipe-session
- **evidence:** Branch entered when session.phase not in ('converged', 'completed'); then: can_continue = can_continue_noop and session.phase in ('converged', 'completed') — the second operand is always False in this branch (invariant of the outer if), so can_continue is always False; response misleads client into thinking extension is impossible when the real condition may differ
- **fix:** Fix the tautological condition: can_continue should reflect whether further swiping is possible given the current state (e.g., pool_remaining > 0), not a phase check that contradicts the branch predicate.
- **verifier:** Confirmed at swipe_service.py:624-638. The noop branch is entered only when `session.phase not in ('converged','completed')` (line 624). Inside it, line 638 computes `'can_continue': can_continue_noop and session.phase in ('converged','completed')` — the second conjunct is the exact negation of the branch guard, so it is provably always False, making `can_continue` always False regardless of `can_continue_noop`. This is a verified tautology, not plausible-but-unverified.  It is a real logic defe…

### `backend/apps/recommendation/services/swipe_service.py:1436` — handle_question_response skip path emits telemetry event before state is persisted
- **dim:** back-swipe-session
- **evidence:** emit_event('question_skipped', ...) called at line ~1436 inside the skip branch, BEFORE the transaction.atomic() block at line ~1450 that saves session state; if the DB save fails the event is already emitted, creating a false audit trail; non-skip path correctly emits inside/after save
- **fix:** Move the emit_event call to after the transaction.atomic() save block in the skip path, or wrap both the save and emit inside the same transaction using transaction.on_commit(lambda: emit_event(...)).
- **verifier:** CONFIRMED but severity overstated. The skip branch in handle_question_response (backend/apps/recommendation/services/swipe_service.py:1436) calls event_log.emit_event('tag_answer', ...) BEFORE the transaction.atomic() save block at :1444-1450, whereas the non-skip path saves first (:1505-1522) then emits at :1527 with an explicit comment "Emit tag_answer event (after save so bias is persisted if event fails)". The ordering inconsistency is real. It is NOT guarded by an outer transaction: Questio…

### `backend/apps/recommendation/views/projects.py:144` — ProjectDetailView uses AllowAny — PATCH/DELETE auth enforced only by manual guard, not DRF framework
- **dim:** back-security · **rule:** Endpoints with AllowAny that mutate state
- **evidence:** permission_classes = [AllowAny] applied at class level, covering GET, PATCH, DELETE. PATCH/DELETE call _get_profile(request) and return 401 if None, but this is manual inline logic. If a future handler is added without the guard, it silently becomes unauthenticated. DRF framework-level enforcement is bypassed.
- **fix:** Split into two views or use IsAuthenticatedOrReadOnly: permission_classes = [IsAuthenticatedOrReadOnly]. Override get_permissions() if GET genuinely needs AllowAny with no auth overhead.
- **verifier:** Finding is accurate and I cannot cite any code that refutes it. backend/apps/recommendation/views/projects.py:144 has `permission_classes = [AllowAny]` at class level, covering GET/PATCH/DELETE. PATCH (lines 173-176) and DELETE (lines 239-242) enforce auth ONLY via inline manual guards — `_get_profile(request)` (returns `getattr(request.user,'profile',None)` per _shared.py:29-30) → 401 if None — plus ownership folded into the queryset (`select_for_update().filter(user=profile)` at 195-198; `filt…

### `backend/apps/recommendation/views/projects.py:163` — saved_ids timestamps and liked_ids intensity scores exposed to non-owners and anonymous users on public boards
- **dim:** back-apps-rest
- **evidence:** ProjectDetailView.get only strips `conversation_history` for non-owners (line 163: `data.pop('conversation_history', None)`). saved_ids shape is `list[{id, saved_at: ISO timestamp}]`; liked_ids shape is `list[{id, intensity: float}]`. Both are returned verbatim to any caller (AllowAny) on public boards. UserProjectsListView (AllowAny) via ProjectListSerializer also returns both fields.
- **fix:** For non-owner responses, also strip saved_ids and replace liked_ids with a count-only field (or strip intensity). Add these to the non-owner pop list alongside conversation_history.
- **verifier:** CONFIRMED in code across all four layers. (1) View: backend/apps/recommendation/views/projects.py:163 — ProjectDetailView.get pops only `conversation_history` for non-owners; nothing strips liked_ids/saved_ids metadata. (2) Serializer: serializers.py:158-159 lists both `liked_ids` and `saved_ids` in ProjectSerializer.Meta.fields, and _LIST_EXCLUDE_FIELDS (line 200) excludes only analysis_report/conversation_history/axis_scores — so both fields ship verbatim from both ProjectSerializer (detail) a…

### `backend/apps/recommendation/views/swipe.py:1` — SwipeView and SessionCreateView lack throttle_classes — no rate limiting on high-cost write paths
- **dim:** back-apps-rest
- **evidence:** SwipeView, SessionCreateView, DiscoveryFeedbackView, DiscoveryFeedView, DiscoveryPromoteView have no `throttle_classes`. Only ReactionView applies ReactionWriteThrottle (60/min). Each swipe triggers Gemini calls + DB writes; unthrottled bulk requests can drive significant cost.
- **fix:** Add a per-user throttle (e.g. 120/min) via `throttle_classes = [UserRateThrottle]` or a custom throttle on SwipeView, SessionCreateView, DiscoveryFeedbackView, and DiscoveryPromoteView.
- **verifier:** VERIFIED at code level. SwipeView (backend/apps/recommendation/views/swipe.py:228-229) and SessionCreateView (views/sessions.py:31-32), plus DiscoveryFeedView/DiscoveryFeedbackView/DiscoveryPromoteView (views/discovery.py), all declare only `permission_classes = [IsAuthenticated]` with ZERO `throttle_classes` (grep count = 0 in all three view files). NO GUARD ELSEWHERE: (1) REST_FRAMEWORK config (config/settings.py:117-145) defines DEFAULT_THROTTLE_RATES but has NO DEFAULT_THROTTLE_CLASSES — in …

### `backend/apps/works/views.py:53` — Works presign accepts image/svg+xml — XSS via R2 CDN serving attacker-controlled SVG
- **dim:** back-security · **rule:** Endpoints accepting dangerous content subtypes
- **evidence:** Validation: if not content_type.startswith('image/'): return 400. Allows image/svg+xml, image/x-portable-pixmap, image/tiff etc. R2 presigned POST condition in storage.py also uses ['starts-with', '$Content-Type', 'image/']. R2 serves objects with the client-supplied Content-Type. SVG can contain <script> tags — XSS if served from a domain shared with the app or a CORS-permissive R2 bucket.
- **fix:** Replace startswith check with an allowlist: ALLOWED_CONTENT_TYPES = {'image/jpeg', 'image/png', 'image/webp', 'image/gif'}. Update storage.py Conditions list to match exact types via Condition literals.
- **verifier:** The core technical claim is confirmed by code. backend/apps/works/views.py:53 `if not content_type.startswith('image/')` accepts image/svg+xml and other dangerous subtypes; backend/apps/works/storage.py:50 sets Fields={'Content-Type': content_type} (client-supplied) and line 53 uses the same permissive `['starts-with','$Content-Type','image/']` condition. R2 serves objects with their stored Content-Type, so an authenticated user can bypass the frontend (which hardcodes image/webp in UploadWorkPa…

### `backend/apps/works/views.py:167` — FinalizeView r2_key individual string length unbounded — only count is capped at MAX_WORK_IMAGES
- **dim:** back-apps-rest
- **evidence:** `if not all(isinstance(key, str) for key in r2_keys)` at line 167 only checks type. The prefix check at line 171-176 (`key.startswith(expected_prefix)`) is the only format constraint. A key like `works/{profile.id}/` + 'A' * 100000 passes both checks, is stored in r2_keys JSONField, and sent to head_object.
- **fix:** Add a per-key length cap after the isinstance check, e.g. `if any(len(k) > 200 for k in r2_keys): return Response({'detail': 'Key too long.'}, status=400)`. R2 keys are at most ~100 chars in the server-generated pattern.
- **verifier:** Mechanically confirmed against source. views.py:167 `if not all(isinstance(key, str) for key in r2_keys)` is a type-only check; views.py:170-172 uses `expected_prefix = f'works/{profile.id}/'` with `key.startswith(expected_prefix)`, a prefix match — so `works/{id}/` + 'A'*100000 passes both. No per-key length cap exists anywhere in the path, the model field is `r2_keys = models.JSONField(default=list)` (models.py:44, no validator), and the key flows to `verify_key_exists` → `s3.head_object(Key=k…

### `docs/algorithm.md:147` — Hyperparameter Space section header claims '12 params' but table has 39 rows
- **dim:** drift-dead-code
- **evidence:** ## Hyperparameter Space (12 params) — counting table rows programmatically yields 39 entries (15 scalar params + 24 flag/config entries). The '12 params' count is from the original sprint scope and was never updated as Topic 01-06, IMP-5/6/7/8, and flag-gated features were added.
- **fix:** Update the section header to '## Hyperparameter Space (39 params)' (or omit the count to avoid future drift).
- **verifier:** Verified factually correct. docs/algorithm.md:147 header reads "## Hyperparameter Space (12 params)". The table beneath (header row 149, separator 150, data rows 151-189) contains exactly 39 data rows — confirmed by two independent counts (grep -cE '^\|' on lines 151-189 = 39). The auditor's decomposition (15 scalar params rows 151-165 + 24 flag/config rows 166-189) sums to 39 and matches. The "12 params" label is stale prose that was never updated as flag-gated features (dpp_*, hyde_*, hybrid_*…

### `frontend/src/App.jsx:20` — LikedOfficesPage imported but never rendered — dead import
- **dim:** drift-dead-code
- **evidence:** import LikedOfficesPage from './pages/LikedOfficesPage.jsx' at line 20, but in the Routes at line 1116: <Route path='my/liked-offices' element={<Navigate to='/my/profile' replace />} />. LikedOfficesPage is never used in any JSX element.
- **fix:** Remove the import statement at line 20. The route already redirects; no JSX uses the component.
- **verifier:** Confirmed with code evidence. frontend/src/App.jsx:20 has `import LikedOfficesPage from './pages/LikedOfficesPage.jsx'` (default import). The route at App.jsx:1116 is `<Route path="my/liked-offices" element={<Navigate to="/my/profile" replace />} />` — it redirects, never rendering the component. Repo-wide grep shows `LikedOfficesPage` appears in App.jsx ONLY as the import binding (no `<LikedOfficesPage>` JSX element anywhere), and App.jsx uses none of the module's named exports (OfficeCard/Skel…

### `frontend/src/App.jsx:85` — Dead state: surprisePending + event listener can never fire because SurpriseBoardModal is never mounted
- **dim:** drift-dead-code
- **evidence:** const [surprisePending, setSurprisePending] = useState(false) + window.addEventListener('archithon:verify-required:surprise-pending', ...) — the event 'archithon:verify-required:surprise-pending' is only dispatched from SurpriseBoardModal.jsx (line 73), which is never imported or rendered. The surprisePending branch at line 1298 can never execute.
- **fix:** Remove the surprisePending useState, the useEffect event listener (lines 168-173), and the else-if branch at lines 1298-1301. The comment at line 83 can also be removed.
- **verifier:** Confirmed dead code. Verified in App.jsx: `surprisePending` useState at line 85, the event-listener useEffect at lines 168-173 (`window.addEventListener('archithon:verify-required:surprise-pending', ...)`), and the unreachable `else if (surprisePending)` branch at lines 1298-1301. The event is dispatched from exactly one place — SurpriseBoardModal.jsx:74 (`window.dispatchEvent(new CustomEvent('archithon:verify-required:surprise-pending'))`, inside a VerifyRequiredError catch). Grep across all of…

### `frontend/src/api/projects.js:95` — getBuildings silently returns [] on any error — callers cannot distinguish network failure from empty result
- **dim:** front-contract
- **evidence:** catch (err) { console.error(...); return [] } — a transient 500 or network timeout causes the calling component to render as if the board has zero buildings; no error state is propagated; getBoardBuildings at line 118 has the same swallow-all pattern
- **fix:** Remove the catch or re-throw the error so callers can set an error UI state. If silent fallback is desired for non-critical paths, at minimum differentiate: return empty array only on 404, re-throw on 5xx/network errors.
- **verifier:** CONFIRMED with code evidence. frontend/src/api/projects.js:107-110 (getBuildings) and 129-132 (getBoardBuildings) both do `catch (err) { console.error(...); return [] }`, swallowing ALL errors. callApi (frontend/src/api/core.js:161-163) throws on any non-ok HTTP (incl. 5xx), and 133-141 throw on network/timeout; the batch endpoint is POST /images/batch/ which gets NO network retries (core.js:122-125, non-idempotent), so a single transient failure throws straight into the swallowing catch — fully…

### `frontend/src/components/SaveBoardModal.jsx:35` — updateProject 403 verify_required not caught in SaveBoardModal — guest board-limit hit shows generic error instead of VerifyGateModal
- **dim:** front-contract
- **evidence:** catch (err) { setError(err?.message || t('board.saveError')) } — backend ProjectDetailView.patch returns 403 {detail:'verify_required', reason:'board_limit_reached'} when a guest tries to save a temp project as permanent after hitting 3-board limit; updateProject() in projects.js also does not catch 403 (unlike createProject which does); VerifyRequiredError + archithon:verify-required event are never dispatched on this path
- **fix:** In SaveBoardModal.handleSave (or in updateProject itself), check if err?.status === 403 && err?.data?.detail === 'verify_required' and dispatch archithon:verify-required CustomEvent + throw VerifyRequiredError, mirroring the createProject pattern at api/projects.js line 30-37.
- **verifier:** CONFIRMED REAL but severity overstated (high→low). The frontend defect is genuine: SaveBoardModal.handleSave (frontend/src/components/SaveBoardModal.jsx:32-38) calls updateProject(projectId,{is_temp:false,...}) and catches all errors generically → setError(err?.message || t('board.saveError')). updateProject (frontend/src/api/projects.js:64-71) re-throws WITHOUT the 403 verify_required handler that createProject has (projects.js:30-37 dispatches archithon:verify-required + throws VerifyRequiredE…

### `frontend/src/components/SurpriseBoardModal.jsx:46` — SurpriseBoardModal hardcodes initial board title as 'Curated for you' (English only, not i18n)
- **dim:** front-components-design · **rule:** i18n rule: new UI strings go through t() + i18n/locales.js
- **evidence:** const [title, setTitle] = useState('Curated for you')  // line 18
This is the default shown before the server response; Korean users see English.
- **fix:** Initialize with t('board.surpise.defaultTitle') (or similar key) and add ko/en entries to locales.js.
- **verifier:** Confirmed at frontend/src/components/SurpriseBoardModal.jsx:18 — `const [title, setTitle] = useState('Curated for you')` hardcodes English as the initial title fallback (shown before/if the server response arrives or if result.title is empty). The codebase has an established i18n system (frontend/src/i18n/locales.js, used by 34+ components) and the "new UI strings go through t()+locales.js" convention is real (MEMORY: i18n new-string convention). This component uses NO t()/i18n at all, so a Kore…

### `frontend/src/components/SurpriseBoardModal.jsx:138` — SurpriseBoardModal uses fontWeight 800, hardcodes #ec4899 gradient, uses skeleton-shimmer, and has no i18n on user-visible strings
- **dim:** front-components-design · **rule:** DESIGN.md §2.5: max weight 700; §8.8: no shimmer; §1.2 accent tokens; i18n rule
- **evidence:** fontWeight: 800  // line 138
background: 'linear-gradient(135deg,#ec4899,#f43f5e)'  // lines 317, 372
className="skeleton-shimmer"  // line 180
>Your saves shaped this — save it as a board?<  // line 146
>Couldn't load. Try again.<  // line 198
>Retry<  // line 216
- **fix:** Cap fontWeight at 700 (DESIGN.md §2.5). Replace hardcoded gradient with var(--accent-1)/var(--accent-2). Replace skeleton-shimmer with lp-skel pulse (DESIGN.md §8.8 forbids shimmer). Add t() calls and locales keys for all user-visible strings.
- **verifier:** All four sub-claims are factually accurate and the cited DESIGN.md rules are genuinely violated in the source: 1. fontWeight: 800 at SurpriseBoardModal.jsx:138 (`<h3 style={{ margin: 0, fontSize: 17, fontWeight: 800 }}>`) — violates DESIGN.md §2.5 (line 124: "Maximum 700. 800/900 forbidden"). 2. Hardcoded gradient `linear-gradient(135deg,#ec4899,#f43f5e)` at lines 317 and 372 — bypasses the §1.2 accent tokens (var(--accent-*)). 3. className="skeleton-shimmer" at line 180 — `.skeleton-shimmer` (i…

### `frontend/src/components/SwipeCard.jsx:332` — SwipeCard image loading state uses skeleton-shimmer animation (forbidden by DESIGN.md)
- **dim:** front-components-design · **rule:** DESIGN.md §8.8: no shimmer animation
- **evidence:** <div className="skeleton-shimmer" style={{ position: 'absolute', inset: 0, background: isDrawingKind ? '#fff' : '#111' }} />  // line 332
- **fix:** Replace with the lp-skel pulse class (or a static neutral bg) while the image loads. DESIGN.md §8.8 explicitly forbids shimmer animation ('No shimmer animation — performance cost + unrelated to Vibe keywords').
- **verifier:** Confirmed with code evidence. SwipeCard.jsx:332 is verbatim `<div className="skeleton-shimmer" style={{ position: 'absolute', inset: 0, background: isDrawingKind ? '#fff' : '#111' }} />`. The `skeleton-shimmer` class IS a genuine animated shimmer: index.css:70-84 defines `@keyframes shimmer` (sliding `background-position: -200%→200%`) applied via `animation: shimmer 1.6s ease-in-out infinite`. DESIGN.md §8.8:645 states verbatim "No shimmer animation (performance cost + unrelated to the Vibe keyw…

### `frontend/src/components/TutorialPopup.jsx:57` — TutorialPopup has no i18n and hardcodes skip/save colors as #ef4444/#ec4899 instead of tokens
- **dim:** front-components-design · **rule:** i18n rule: new UI strings through t(); DESIGN.md §1.2: accent tokens; §1.3: destructive token
- **evidence:** >Swipe Left< >Swipe Right< >Left Arrow< >Right Arrow< >Tap Card< >View details< >Tap anywhere to continue< — none wrapped in t()  // lines 57-106
color: '#ef4444'  // lines 58, 73, 79
stroke: '#ec4899'  // line 62, 83
- **fix:** Add useTranslation and wrap all user-visible strings in t() calls with locales keys. Replace '#ef4444' with 'var(--color-destructive)' and '#ec4899' with 'var(--accent-1)'.
- **verifier:** Verified against frontend/src/components/TutorialPopup.jsx. i18n half is solidly real: 7 user-visible English strings are hardcoded (lines 57 "Swipe Left", 58 "Skip", 66 "Swipe Right", 67 "Save", 78 "Left Arrow", 79 "Skip", 88 "Right Arrow", 89 "Save", 101 "Tap Card", 102 "View details", 106 "Tap anywhere to continue"), none wrapped in t(). The repo has an established i18n system (src/i18n/index.js + src/i18n/locales.js) used by 30 of 72 jsx files, and grep found NO existing tutorial locale keys…

### `frontend/src/components/profile/ArticleCard.jsx:18` — ArticleCard uses onMouseEnter/Leave state-mutation for hover and hardcodes #ec4899 accent color
- **dim:** front-components-design · **rule:** DESIGN.md §4: hover via CSS :hover only, inline JS hover pattern retired
- **evidence:** borderLeft: '3px solid #ec4899',  // line 18
onMouseEnter: e.currentTarget.style.borderLeftColor = '#f43f5e'  // line 26
color: '#ec4899',  // line 65 (source badge)
- **fix:** Move hover transitions to a co-located ArticleCard.module.css using the CSS :hover pseudo-class (DESIGN.md §4). Replace '#ec4899'/'#f43f5e' with 'var(--accent-1)' throughout. The article left-border accent is supposed to be theme-aware.
- **verifier:** Code facts confirmed verbatim at frontend/src/components/profile/ArticleCard.jsx: line 18 `borderLeft: '3px solid #ec4899'`, lines 23-34 inline onMouseEnter/onMouseLeave mutating style (`#f43f5e` at L26, `#ec4899` at L32), line 65 `color: '#ec4899'`. The cited DESIGN.md §4 rule is real (DESIGN.md:239-241: "visual transitions ... use only the CSS `:hover` pseudo-class. The inline `onMouseEnter/Leave` + React-state pattern is retired"). So this IS a genuine style-guide deviation — verdict=real.  B…

### `frontend/src/components/profile/BioPersonaFlipCard.jsx:72` — BioPersonaFlipCard hardcodes 'tap to reveal persona', 'Styles', 'Programs' without i18n
- **dim:** front-components-design · **rule:** i18n rule: new UI strings go through t() + i18n/locales.js
- **evidence:** >tap to reveal persona<  // line 72
>Styles<  // line 126
>Programs<  // line 150
- **fix:** Add useTranslation and wrap in t() calls with locales keys. Low severity because this is a profile-page component seen by Korean-first users.
- **verifier:** Verified in frontend/src/components/profile/BioPersonaFlipCard.jsx: line 72 renders literal "tap to reveal persona", line 126 "Styles", line 150 "Programs" — all hardcoded JSX text with no t() wrapping. File imports only useState (line 1); no useTranslation. The i18n infra exists (src/i18n/index.js, src/i18n/locales.js) and the sibling extracted profile components all follow the rule: ProfileQr.jsx:20, EditCardForm.jsx:24, BoardCard.jsx:5 all import useTranslation. Grep of locales.js confirms th…

### `frontend/src/components/profile/BioPersonaFlipCard.jsx:101` — BioPersonaFlipCard uses onMouseEnter/Leave React state and hardcodes #ec4899 in gradient and chip colors
- **dim:** front-components-design · **rule:** DESIGN.md §4: hover via CSS :hover only; §1.2: accent colors are themed
- **evidence:** background: 'linear-gradient(135deg, #ec4899, #f43f5e)',  // line 101
color: '#ec4899',  // lines 105, 134, 158
background: 'rgba(236,72,153,0.15)',  // lines 133, 157
onMouseEnter/Leave with setIsHovered  // lines 24-25
- **fix:** Replace hardcoded #ec4899 / #f43f5e with var(--accent-1) / var(--accent-2) and rgba(236,72,153,.15) with color-mix(in srgb, var(--accent-1) 15%, transparent). Extract the lift transform to a CSS module :hover rule.
- **verifier:** Code confirmed verbatim at cited lines in frontend/src/components/profile/BioPersonaFlipCard.jsx: line 101 `background: 'linear-gradient(135deg, #ec4899, #f43f5e)'`; lines 105/134/158 `color: '#ec4899'`; lines 133/157 `background: 'rgba(236,72,153,0.15)'`; lines 24-25 `onMouseEnter/onMouseLeave` with `setIsHovered` driving a React-state `translateY(-4px)` lift (line 21). The DESIGN.md rules cited are real: §1.2 accents are themed tokens (tokens.css defines --accent-1=#0969DA / --accent-2=#8250DF…

### `frontend/src/components/profile/DescriptionAboutFlipCard.jsx:24` — DescriptionAboutFlipCard uses onMouseEnter/Leave React state for lift hover transition
- **dim:** front-components-design · **rule:** DESIGN.md §4: hover via CSS :hover only
- **evidence:** onMouseEnter={() => setIsHovered(true)}  // line 24
onMouseLeave={() => setIsHovered(false)}  // line 25
transform: isHovered ? 'translateY(-4px)' : 'translateY(0)',  // line 21
- **fix:** Extract to a DescriptionAboutFlipCard.module.css and use .card:hover { transform: translateY(-4px); } — the transition is already declared in inline style so this is a safe swap.
- **verifier:** Confirmed verbatim at frontend/src/components/profile/DescriptionAboutFlipCard.jsx:20-25: `transform: isHovered ? 'translateY(-4px)' : 'translateY(0)'` (line 21) driven by React state via `onMouseEnter={() => setIsHovered(true)}` (24) / `onMouseLeave={() => setIsHovered(false)}` (25), with the transition already declared inline (line 20). This directly violates DESIGN.md §4 (lines 239-241): 'visual transitions (color, shadow, transform) use only the CSS :hover pseudo-class. The inline onMouseEnt…

### `frontend/src/components/profile/ProjectCard.jsx:37` — ProjectCard hover uses onMouseEnter/Leave mutation with hardcoded #ec4899 border
- **dim:** front-components-design · **rule:** DESIGN.md §4: hover via CSS :hover only; §1.2: accent colors are themed
- **evidence:** onMouseEnter: e.currentTarget.style.borderColor = 'rgba(236,72,153,0.55)'  // line 39
onMouseLeave: e.currentTarget.style.borderColor = 'transparent'  // line 43
- **fix:** Move hover styles to ProjectCard.module.css; use CSS :hover to set border-color to color-mix(in srgb, var(--accent-1) 55%, transparent). Removes the JS pattern and makes it theme-aware.
- **verifier:** CONFIRMED violation, but severity overstated. Code verified at /Users/kms_laptop/Documents/archi-tinder/make_web/frontend/src/components/profile/ProjectCard.jsx:37-44 — onMouseEnter sets e.currentTarget.style.borderColor = 'rgba(236,72,153,0.55)' (line 39), onMouseLeave resets to 'transparent' (line 43), exactly as claimed. (1) Rule violation is real: DESIGN.md §4 lines 239-241 explicitly state hover visual transitions must use ONLY the CSS :hover pseudo-class and that 'The inline onMouseEnter/L…

### `frontend/src/index.css:75` — skeleton-shimmer animation class is defined in index.css and used in 6+ components despite DESIGN.md §8.8 explicitly forbidding shimmer
- **dim:** front-components-design · **rule:** DESIGN.md §8.8: No shimmer animation
- **evidence:** .skeleton-shimmer { animation: shimmer 1.6s ease-in-out infinite; }  // lines 75-83
Used in SwipeCard.jsx:332, SurpriseBoardModal.jsx:180, SaveToBoardModal.jsx:268, ResultsPage.jsx:42/304/360
- **fix:** Remove the skeleton-shimmer @keyframes + class from index.css. Replace all usages with the existing lp-skel pulse class (already defined in index.css, already used by CardSkeleton.jsx). The pulse is lower-cost and matches the Vibe keywords.
- **verifier:** CONFIRMED. index.css:75-84 defines `.skeleton-shimmer { animation: shimmer 1.6s ease-in-out infinite; }` driven by `@keyframes shimmer` (background-position sweep, lines 70-73) — a classic shimmer. DESIGN.md §8.8 line 645 states verbatim: "No shimmer animation (performance cost + unrelated to the Vibe keywords)." The compliance table (DESIGN.md:745) even falsely marks Loading State "(no shimmer) — §8.8 ✅", contradicting the actual code. The class is live (not dead code), rendered in 13 usages ac…

### `frontend/src/pages/BuildingDetailPage.jsx:190` — BuildingDetailPage uses fontWeight 800 in five locations, violating 700-maximum rule
- **dim:** front-components-design · **rule:** DESIGN.md §2.5: max font-weight is 700; 800/900 forbidden
- **evidence:** fontWeight: 800  // lines 190, 200, 235, 494, 518
- **fix:** Change all fontWeight: 800 to fontWeight: 700 (the maximum permitted per DESIGN.md §2.5).
- **verifier:** Confirmed with primary evidence. frontend/src/pages/BuildingDetailPage.jsx has exactly 5 `fontWeight: 800` occurrences at the cited lines 190, 200, 235, 494, 518 (quoted directly from the file). DESIGN.md:124 §2.5 states verbatim "Maximum 700. 800/900 forbidden.", reinforced by the summary table at DESIGN.md:724 ("Font-weight cap 700 (800/900 forbidden)"). The h1 hero at line 200 additionally violates the §2.4 h1-weight:700 cap. No guard exists: frontend/eslint.config.js has no font-weight lint …

### `frontend/src/pages/ResultsPage.jsx:72` — ResultsPage uses fontWeight 800 and skeleton-shimmer animation
- **dim:** front-components-design · **rule:** DESIGN.md §2.5; §8.8
- **evidence:** fontWeight: 800  // line 72
className="skeleton-shimmer"  // lines 42, 304, 360
- **fix:** Lower fontWeight to 700. Replace skeleton-shimmer with lp-skel pulse animation per DESIGN.md §8.8 ('no shimmer animation').
- **verifier:** Both violations confirmed with primary evidence. (1) frontend/src/pages/ResultsPage.jsx:72 sets fontWeight: 800 on the #{rank} badge; DESIGN.md §2.5 (lines 124-125) states "Maximum 700. 800/900 forbidden," and the compliance table (line 724) marks "Font-weight cap 700 (800/900 forbidden)" as enforced. Line 72 is the sole outlier in the file (all other weights are 600/700). (2) ResultsPage.jsx:42, 304, 360 use className="skeleton-shimmer", which (index.css:75-84) applies "animation: shimmer 1.6s …

### `frontend/src/pages/firmProfile/FirmArticlesSection.jsx:17` — FirmArticlesSection hardcodes 'Featured Articles' without i18n
- **dim:** front-components-design · **rule:** i18n rule: new UI strings go through t() + i18n/locales.js
- **evidence:** >Featured Articles<  // line 17
- **fix:** Add useTranslation and t('firm.articles.header') with locales entries. Low priority since Phase 18 (articles) is deferred.
- **verifier:** Confirmed at frontend/src/pages/firmProfile/FirmArticlesSection.jsx:17: the <h3> contains the literal `Featured Articles` with no useTranslation import and no t() wrapper; no `firm.articles` key exists in frontend/src/i18n/locales.js. The i18n convention is real and broadly adopted (22 page-context files + many components import useTranslation), so this is a genuine deviation from an in-force rule, not a phantom rule — hence not a false-positive.  Two mitigations bound the severity but don't ref…

### `web-testing/research/persona.py:151` — LLM mode uses legacy google-generativeai SDK (deprecated) while backend uses google-genai
- **dim:** drift-dead-code
- **evidence:** import google.generativeai as genai + genai.GenerativeModel('gemini-2.5-flash') — web-testing/requirements.txt pins google-generativeai>=0.3.0 (legacy SDK), but backend/requirements.txt uses google-genai>=1.0 (the new unified SDK). The two SDKs have incompatible APIs.
- **fix:** Migrate web-testing/research/persona.py to use google.genai (import google.genai as genai; genai.configure(api_key=...); genai.GenerativeModel(...)) and update web-testing/requirements.txt to google-genai>=1.0.
- **verifier:** Factually verified. web-testing/research/persona.py:151 imports `google.generativeai as genai` (legacy SDK) and :176-177 call the legacy API `genai.configure(api_key=...)` + `genai.GenerativeModel('gemini-2.5-flash')`. web-testing/requirements.txt:2 pins `google-generativeai>=0.3.0` (the vendor-deprecated SDK), while backend/requirements.txt:10 pins `google-genai>=1.0` (the new unified SDK), which the backend uses via `from google import genai` (backend/apps/recommendation/services/_gemini.py:12…

### `web-testing/research/scenarios.py:107` — Module-level decide_swipe() function is dead code — build_scenario() uses tracker.decide instead
- **dim:** drift-dead-code
- **evidence:** def decide_swipe(card_metadata: dict, persona: PersonaProfile) -> str: at line 107 — build_scenario() at line 132 assigns decide_swipe=tracker.decide (the _SwipeTracker method, not this function). No other file imports or calls the module-level decide_swipe.
- **fix:** Delete the module-level decide_swipe() function (lines 107-114) or document it as an alternative entry point. If deleted, update the TestScenario.decide_swipe type annotation comment.
- **verifier:** Verified in web-testing/research/scenarios.py. Module-level decide_swipe() (lines 107-114) is genuinely dead. build_scenario() (line 132) assigns decide_swipe=tracker.decide (the _SwipeTracker.decide bound method, line 83), and the runner at runner.py:936 invokes scenario.decide_swipe — the TestScenario instance attribute set to tracker.decide, NOT the module function. Grep across the repo shows the only import from the module is build_scenario (run.py:29); the module-level decide_swipe is never…

### `web-testing/runner/runner.py:23` — FRONTEND_URL and BACKEND_URL are hardcoded constants with no env-var override
- **dim:** drift-dead-code
- **evidence:** FRONTEND_URL = 'http://localhost:5174' and BACKEND_URL = 'http://localhost:8001' — these cannot be overridden without editing the file. The backend port 8001 is Django's non-default and differs from the standard :8000, making dual-machine or CI use brittle.
- **fix:** Replace with os.getenv('FRONTEND_URL', 'http://localhost:5174') and os.getenv('BACKEND_URL', 'http://localhost:8001') so CI or multi-machine setups can override via environment variables.
- **verifier:** Confirmed at web-testing/runner/runner.py:23-24: FRONTEND_URL='http://localhost:5174' and BACKEND_URL='http://localhost:8001' are literal string constants with no os.getenv wrapper. They feed DEV_LOGIN_ENDPOINT (line 25) and page.goto (line 471). No override path exists: run_test() signature (line 426) is (scenario, run_id, reports_dir) with no URL param, and run.py:183 calls it with only those three args (run.py has no getenv/environ/argparse for these URLs). Grep across web-testing/ shows the …

### `web-testing/runner/runner.py:830` — Stale text selector 'Your Taste is Found' never matches; actual action-card text is 'Your taste is taking shape!'
- **dim:** drift-dead-code
- **evidence:** page.locator('text=Your Taste is Found') at line 830 — locales.js line 504 has swipe.actionCard.message = 'Your taste is taking shape!' (en). The string 'Your Taste is Found' does not exist anywhere in the frontend source.
- **fix:** Change the locator to match the actual i18n string, e.g. page.locator('text=Your taste is taking shape') or detect convergence via the API response field is_analysis_completed instead of DOM text.
- **verifier:** CONFIRMED. runner.py:830 is `page.locator('text=Your Taste is Found')` and runner.py:832 logs the same string. A full-repo grep (py/js/jsx/ts/tsx/json, excl. node_modules) shows 'Your Taste is Found' exists ONLY in runner.py — it is produced by NO frontend, backend, locale, or fixture. The action card's real text comes from SwipePage.jsx:18 `message = card.action_card_message || t('swipe.actionCard.message')`: backend supplies `'취향이 충분히 모였어요!'` (swipe_service.py:93), en i18n fallback is `'Your t…


## UNCERTAIN (1)

### `docs/algorithm.md:147` — 34 RECOMMENDATION dict params in settings.py are absent from the algorithm.md Hyperparameter Space table
- **dim:** drift-dead-code
- **evidence:** settings.py RECOMMENDATION dict has 70+ top-level keys; algorithm.md table covers only 39. Missing families: discovery_* (14 params: chunk_size, tier2/tier3 counts, dislike thresholds, centroid TTL, tablesample_pct, etc.), question_* (9 params: max_per_session, cooldown_swipes, boost/penalty weights, TF-IDF params, fast-swipe detection), llm_search_* (6 params: topk, w_bm25, priority_boost, etc.), card_cache_ttl, recent_latencies_cap.
- **verifier:** The raw FACT is code-verified airtight: settings.py RECOMMENDATION dict has 73 top-level keys (settings.py:282-406); ~34 of them (16 discovery_*, 9 question_*, 6 llm_search_*, plus card_cache_ttl/recent_latencies_cap/corpus_df_cache_ttl_seconds) appear NOWHERE in the algorithm.md Hyperparameter Space table (grep for discovery_/question_/llm_search_/card_cache_ttl/recent_latencies_cap in docs/algorithm.md = 0 hits). The claim's counts are accurate (question=9, llm_search=6, 34-total all exact; discovery undercounted 14 vs actual 16). But the FINDING (as framed + its fix) does not survive skepti


## Refuted (33) — verified false positives, do not re-chase

- [back-engine] `backend/apps/recommendation/engine.py:835` — Tier 2 relaxation silently skips to random pool when filters contain only location/year fields → The mechanical observation is correct — at engine.py:835 `if relaxed and relaxed != (filters or {}):`, a geo/numeric-only filter set relaxes to `relaxed={}` (falsy) so the tier-2 branch is skipped and control falls to ti
- [back-engine] `backend/apps/recommendation/engine.py:1360` — _building_embedding_cache FIFO eviction has a race condition that can raise KeyError → The code at backend/apps/recommendation/engine.py:1358-1361 matches the claim exactly (snapshot keys via list(...) then del each). But the claimed KeyError race requires two concurrent executions of this eviction loop ag
- [back-engine] `backend/apps/recommendation/engine.py:1727` — get_dislike_fallback divides by zero norm when dislike vectors cancel to zero mean → Code matches the claim exactly at backend/apps/recommendation/engine.py:1727 — `dislike_centroid = dislike_centroid / np.linalg.norm(dislike_centroid)` with no zero-norm guard, and I verified the mechanical outcome (zero
- [back-engine] `backend/apps/recommendation/engine.py:1776` — get_top_k_mmr zero-norm division on round_num=None path produces NaN centroid → The static code read is accurate but the branch is unreachable in production — this is a false positive killed by caller analysis.  DECISIVE REFUTATION (fails task criterion #4, reachable path): The unguarded division at
- [back-engine] `backend/apps/recommendation/engine.py:1307` — Seed injection elevates score but not pool position when seed already in DB results → The code is quoted accurately (engine.py:1307-1312 and parallel sites 1075-1079/1173-1179/1218-1224: when a seed is already in pool_scores, insert(0,sid) is skipped but pool_scores[sid]=1.1 is set). But the claim's CONCL
- [back-engine] `backend/apps/recommendation/engine.py:304` — get_building_card calls _row_to_card twice on cache miss — wasteful → Refuted by code evidence. engine.py:304-306 does call _row_to_card twice on a cache miss (line 304 for the return, line 306 for the cache), so the observable fact is real — but the auditor's rationale ("both produce the 
- [back-swipe-session] `backend/apps/recommendation/views/swipe.py:248` — Empty-string idempotency_key causes all subsequent swipes to be silently dropped as duplicates → The claim's two premises are factually correct — swipe.py:248 defaults idempotency_key to '' and models.py:136 has unique_together=[('session','idempotency_key')]. And the collision mechanism does exist in code: the read
- [back-swipe-session] `backend/apps/recommendation/services/swipe_service.py:799` — like_vectors missing dedup guard allows duplicate embedding entries and double-weights taste centroid → The claim's code observation is literally accurate: swipe_service.py:799 appends to session.like_vectors with NO `canonical_bld_id not in ...` dedup, unlike its siblings liked_ids (line 784) and disliked_ids (line 803). 
- [back-swipe-session] `backend/apps/recommendation/services/swipe_service.py:1542` — handle_question_response MMR flush uses stale exposed_ids from first transaction for next-card selection → The mechanism is partially real at the code level: `session.exposed_ids` at swipe_service.py:1545 is a Python-object snapshot from the first transaction (line 1524), and the MMR loop (1550-1565) filters on it; the second
- [back-swipe-session] `backend/apps/recommendation/services/session_service.py:204` — create_session dedupe path returns first-ever card instead of current card on retry after progress → The literal code reading is accurate: session_service.py:205-208 returns initial_batch[0] as next_image unconditionally while line 229 returns the real _progress(existing_session) — internally inconsistent by constructio
- [back-swipe-session] `backend/apps/recommendation/services/swipe_service.py:297` — _pick_pool_category makes buildings DB query while holding select_for_update row locks → The buildings-DB query in _pick_pool_category (swipe_service.py:309-320) does run inside the locked transaction (atomic opened at :727, select_for_update on AnalysisSession :729 and Project :735; call chain :1055 _check_
- [back-llm] `backend/apps/recommendation/views/search.py:1` — prior_filters keys not allowlisted before Gemini context injection — prompt injection vector → The auditor's CODE FACT is accurate but the SECURITY CLAIM is refuted. Verified: search.py:40-53 `_clean_filters()` only drops nullish values (via `_clean_filter_value`, line 30-37) and never allowlists keys, so any key 
- [back-llm] `backend/apps/recommendation/views/search.py:1` — conversation_history allows role='model' — users can spoof fake LLM turns → The `role='model'` allowance is intentional and load-bearing, not a vulnerability. (1) It is required by the multi-turn chat design: the backend is stateless per parse-query request, so the frontend maintains the convers
- [back-llm] `backend/apps/recommendation/services/generation.py:1` — Unvalidated LLM JSON stored and re-fed to image generation prompt — chained prompt injection → Code facts are accurate but the "chained prompt injection" security framing is refuted by the trust boundary. SOURCE is not attacker-controlled: generate_persona_report builds its prompt (generation.py:243-251) entirely 
- [back-auth] `backend/apps/accounts/serializers.py:374` — PATCH handle=null clears handle without syncing display_name, breaking the handle==display_name invariant → The mechanical trace is correct: PATCH {handle: null} → validate_handle returns None (serializers.py:375-376), update() computes unified=None (handle and display_name both None at lines 388-404), the sync block is skippe
- [back-auth] `backend/apps/accounts/views/auth.py:355` — GuestPromoteView email lookup uses exact case match instead of iexact, inconsistent with LinkEmailView → Code facts are accurate: auth.py:355 uses `user__email=` (exact) while auth.py:1010 uses `user__email__iexact=`. But the stated IMPACT ("would not find the collision that LinkEmailView would catch") fails reachability. (
- [back-auth] `backend/apps/accounts/views/auth.py:768` — RegisterView catches bare Exception on user creation; non-uniqueness DB errors may be misreported as 'ID already taken' → The claimed harm ("misreported as 'ID already taken'" / "mask real errors") does not materialize. In backend/apps/accounts/views/auth.py:768-773 the bare `except Exception:` gates the 400 response behind `if UserProfile.
- [back-auth] `backend/apps/accounts/views/auth.py:385` — GuestPromoteView Branch 1 blacklists tokens that are CASCADE-deleted when guest_user is deleted, making the blacklist loop vacuous → The claim's core mechanism is factually wrong. It asserts TWO cascade links; only one is real. Link #2 (BlacklistedToken → OutstandingToken, CASCADE) is correct: token_blacklist/models.py:40 `token = models.OneToOneField
- [back-auth] `backend/apps/accounts/views/auth.py:912` — SetPasswordView silently swallows ImportError for token_blacklist, masking any actual import failure → Code exists as described (auth.py:912-919: try-import OutstandingToken/BlacklistedToken, except ImportError: pass), but the claim's load-bearing harm assertion — "masking any actual import failure" / hiding the AUTH-CRIT
- [back-apps-rest] `backend/apps/recommendation/services/session_service.py:238` — Guest board-limit counts is_temp boards in session_service but not in discovery views — inconsistent gate → The discrepancy exists but is NOT a functional bug — each gate is correct for what it guards, and the auditor's proposed fix would introduce a real hole. Code evidence:  - session_service.py:372 mints boards with `is_tem
- [back-apps-rest] `backend/apps/social/views.py:199` — UserSavedStudiosView exposes any user's architect follow list to any authenticated caller — no visibility policy documented → The claim's core evidence is factually contradicted by the code. (1) The claim says the handler accepts a `username` path param, but views.py:199 reads `def get(self, request, user_id):` and urls.py:9 routes `users/<int:
- [back-security] `backend/apps/accounts/views/auth.py:492` — Google OAuth token exchange response body logged — includes id_token → The claim's core evidence is materially wrong about what reaches the log line. At backend/apps/accounts/views/auth.py:492 the warning `logger.warning('Google token exchange returned no access_token: %s', token_data)` fir
- [back-security] `backend/apps/profiles/serializers.py:75` — OfficeAdminSerializer fields='__all__' exposes privileged Office flags → The claim is factually accurate that OfficeAdminSerializer uses fields='__all__' on a model with privileged flags (models.py:11 verified, :19 claim_status, :29 canonical_id, :34 is_recommendable). But as a SECURITY findi
- [back-security] `backend/apps/accounts/throttling.py:1` — CheckHandleThrottle scope 'check_handle' absent from DEFAULT_THROTTLE_RATES — rate not operator-overridable → The claim's load-bearing justification — "Inconsistent with all other throttle classes in the codebase" — is factually inverted. In backend/apps/accounts/throttling.py, ALL 8 throttle classes set `rate` as a class-level 
- [back-models-migrations] `backend/apps/recommendation/models.py:187` — SessionEvent.session FK uses CASCADE — silently destroys all telemetry when a session is purged → The code fact is accurate — models.py:187-190 has `session = ForeignKey(AnalysisSession, on_delete=CASCADE, null=True, blank=True)`, and cascade-delete paths exist (Project→sessions CASCADE at :74; purge_legacy_projects;
- [back-models-migrations] `backend/apps/recommendation/models.py:7` — Project model has no composite index for the two most common list queries → The structural observation is accurate (Project, models.py:7, has no Meta class → only the auto FK index on user_id; no AddIndex migration exists), but the claim fails the "would it trigger in practice" and "guarded else
- [back-models-migrations] `backend/apps/recommendation/models.py:23` — Project.report_image TextField has no size constraint — could store arbitrarily large base64 blobs → The filed claim's thesis — "no size constraint → could store ARBITRARILY LARGE base64 blobs → add MaxLengthValidator / validate_report_image / cap len > 1_000_000" — is refuted on three independent grounds with code evid
- [back-models-migrations] `backend/apps/recommendation/migrations/0011_add_hyde_topic03.py:1` — Migration 0011 renames v_initial field but also runs AlterField on event_type choices — mixing schema and choices change in one migration increases rollback complexity → Refuted on the core technical premise. File backend/apps/recommendation/migrations/0011_add_hyde_topic03.py lines 12-23 do bundle AddField(v_initial) + AlterField(sessionevent.event_type), so the surface description is p
- [back-models-migrations] `backend/apps/accounts/migrations/0011_login_onboard_1_unified_id_and_is_guest.py:103` — Data migration loads all UserProfile rows into memory at once via list() → Code quote is accurate: backend/apps/accounts/migrations/0011_...py lines 103-105 do `profiles = list(UserProfile.objects.select_related('user').all())` with no pagination. But the impact is unreachable. (1) This is a on
- [front-contract] `frontend/src/api/architects.js:1` — unfollowArchitect swallows 404 — already-unfollowed state masked as success → The claim mischaracterizes the code and its proposed fix is a no-op. Actual code (frontend/src/api/architects.js:31-38): unfollowArchitect catches errors but only returns {} for a 404 and RE-THROWS every other status (`t
- [front-components-design] `frontend/src/layouts/MainLayout.jsx:46` — MainLayout logout button uses onMouseEnter/Leave with hardcoded #f87171 color and CTA button hardcodes #ec4899 gradient; UI strings not i18n-ized → Facts verified against source — all four lines match exactly: MainLayout.jsx:46 inline onMouseEnter with hardcoded '#f87171'; :99 hardcoded 'linear-gradient(135deg,#ec4899,#f43f5e)'; :94/:104 un-i18n'd English strings. T
- [front-components-design] `frontend/src/components/profile/ArticleCard.jsx:8` — ArticleCard renders article.url from API directly as href with no protocol check — javascript: URI injection possible when articles go live → The code claim is literally accurate — ArticleCard.jsx:8 renders `href={article.url}` with no protocol/scheme validation. But the finding fails the reachability check: the `article.url` data path is dead in the current c
- [front-components-design] `frontend/src/components/profile/ProjectCard.jsx:135` — ProjectCard renders hardcoded 'Project' sub-label and 'CITY'/'YEAR' column labels without i18n → Code is quoted accurately — lines 135/139/140 of frontend/src/components/profile/ProjectCard.jsx do hardcode "Project", "CITY", "YEAR" outside t(). But the finding is refuted on two grounds:  1. DOCUMENTED INTENTIONAL DE


## Per-dimension notes (coverage + caps)

### rules-buildings
One confirmed violation (medium): hydration.py _batch_fetch_embeddings omits the is_publishable gate with a comment justifying the omission. All other ~20 call sites across 12 files correctly gate. db_router blocks migrations on 'buildings'. No ORM binding to canonical_v2_buildings. No user-input SQL injection vectors found — all f-string column/axis interpolations use hardcoded allowlists or settings-derived numeric values. canonical_v2_architects queries correctly use is_recommendable (the correct publishability column for that table, not is_publishable) — not a violation.

### back-engine
Hard rule compliance (canonical_bld_id, connections['buildings'], is_publishable gate, no raw redis-py, no ORM on canonical_v2_buildings): CLEAN across all examined files. No critical or high findings. DPP bug (finding #1) affects the dpp_topk_enabled=True path only; prod setting dpp_topk_enabled=False so it is latent, not live. Tier 2 relaxation skip (finding #2) is a real user-facing edge case for location-only filter users. Race condition (#3) is low-probability under GIL but real under concurrent gunicorn workers sharing the module-level dict. Total 7 findings; none dropped.

### back-swipe-session
9 findings total, all reported. Severity map: 3 high (idempotency empty-string hole, two TOCTOU races on session state), 4 medium (like_vectors dedup, event-before-state, stale exposed_ids in MMR flush, wrong card on session retry), 2 low (always-False can_continue, buildings-DB query inside lock). KNOWN-ACCEPTED items (prefetch thread daemon pattern, _connections.close_all() usage, CONN_MAX_AGE on buildings DB, IMP-8 cache race, question_state in-memory mutation included in update_fields) were verified and not reported. SQL injection in _compute_kw_vec_refine confirmed safe via _VALID_AXES frozenset guard before any f-string interpolation. Async telemetry and prefetch threads confirmed thread-safe: both snapshot all inputs before thread spawn, call _connections.close_all() at start, no shared mutable state after handoff. transaction.on_commit() for _async_board_name_update confirmed correct pattern.

### back-llm
6 findings total, none dropped. Severity rationale: two high findings (no rate limiting on main LLM endpoint + prior_filters prompt injection) are real exploitable vectors with direct cost/security impact. Two medium findings (role spoofing + chained prompt injection via stored LLM JSON) require multi-step exploitation. One additional medium (reports endpoint rate limiting) is grouped separately from the search endpoint finding for clarity. One low finding (singleton race) is functionally benign but technically incorrect. Known-accepted items from the audit spec were correctly excluded: buildings DB CONN_MAX_AGE omission, SQLite conftest override, dual DATABASES aliases, sole-admin CODEOWNERS. The generate_persona_report() call to generate_content_with_fallback() omits an explicit timeout= argument — the default in the SDK resolves to 15.0s from _retry_gemini_call's signature, so this is acceptable behavior rather than a gap; excluded from findings.

### back-auth
10 findings total, all reported. No findings were dropped. Key areas verified: JWT blacklist is enabled (INSTALLED_APPS + SIMPLE_JWT settings), token rotation is configured correctly, email_verified gating on account-linking is solid, NFC normalization in handle validation is correct, timing equalization in PasswordLoginView is valid (dummy hash runs regardless), is_guest claim correctly propagates to access tokens (simplejwt copies all non-excluded claims from refresh to access), signal backstop for cache invalidation is properly wired via AppConfig.ready(), and the CachedJWTAuthentication TTL cap is correctly bounded to the access token lifetime. The most actionable findings are: (1) email_verified_at not set in GuestPromoteView Branch 2, causing frontend inconsistency; (2) no throttle on social login and token refresh endpoints; (3) implicit-flow access_token still accepted by GoogleLoginView.

### back-apps-rest
6 findings total, all reported. No findings dropped. IDOR checks clean across notifications (self-only), session ownership (user= filter), work listing (owner= filter), PATCH/DELETE on projects (select_for_update + user= filter). Buildings DB always accessed read-only via raw SQL; is_publishable=true gated at every query site found. URL patterns all have trailing slashes. No raw redis-py usage found. The saved_ids/liked_ids exposure (finding 3) is the most impactful correctness issue — AllowAny on ProjectDetailView means unauthenticated users can read bookmark timestamps and preference intensity for any public board. Guest limit inconsistency (findings 1+2) is a logic gap where the comment in session_service.py explicitly claims to mirror discovery views but uses a different filter.

### back-security
No raw SQL injection found: all cursor.execute and RawSQL usages use %s parameterization; axis names come from static server-side allowlists. No SSRF: all outbound HTTP targets hardcoded provider URLs (Google/Kakao/Naver/HuggingFace router) or server-config model names. DEBUG defaults False, CORS defaults localhost, production security block (HSTS, secure cookies) correctly gated on not DEBUG — no SECURE_SSL_REDIRECT but this is typically handled at Railway/Vercel reverse proxy layer. DevLoginView is AllowAny but gated to DEBUG=True URL registration and requires DEV_LOGIN_SECRET — per known-accepted list. GuestLoginView and RegisterView are AllowAny with state mutation but both have explicit throttle_classes — acceptable. No raw redis-py calls anywhere; all cache access via django.core.cache abstraction per INFRA-REDIS-1 rule.

### back-models-migrations
No duplicate migration numbers found in any app. All migration chains are complete with no gaps (accounts 0001-0011, recommendation 0001-0030, social 0001-0007, profiles 0001-0005, works 0001-0002, notifications 0001). JSONField(default=dict) and JSONField(default=list) are callables — safe for Django. The like_vectors field no longer stores full 384-dim embeddings in the DB row (only {id, round} since swipe_service.py line 799) — no unbounded growth concern. The 0021 migration fixture path resolves correctly via os.path.dirname(__file__). AnalysisSession user/project FKs are always created together with the same profile (session_service.py:397) — no cross-user mismatch risk in practice. The notif_recip_readat_idx on (recipient, read_at) correctly covers the unread-count filter(recipient=X, read_at__isnull=True). ArchitectFollow indexes are non-redundant: db_index on architect_id serves reverse lookups; unique_together serves paired lookups; Meta Index on (follower, followed_at) serves ordered following list.

### front-contract
7 findings total (under 20 cap, none dropped). Severity rationale: 3 high = real user-facing bugs (Hangul handle blocked at UI, guest board-limit modal never appears, savedIds.has() always false on both profile pages); 2 medium = silent error swallowing that can silently corrupt user-visible state; 1 low = optimistic-UI revert gap. Intentional patterns confirmed and excluded: getResult predicted_images→predicted_like_images normalization (correct); client.js barrel is additive not exhaustive (direct sub-module imports are fine); single-flight _refreshPromise token refresh (correct); LikedBuildingsView POST returns {liked_count} not the card (frontend liked.js does not consume it); AvatarUploadView returns UserSerializer (frontend only reads avatar_url); two DATABASES aliases (intentional architecture). AccountScreen line 104 has a comment noting backend also validates — but the client-side false rejection for Hangul/2-char IDs still blocks user action before the server ever sees the request.

### front-pages
No violations of hard backend rules (canonical_bld_id, buildings DB ORM, is_publishable gate, raw redis-py, trailing slash). No Tailwind/Bootstrap/MUI found. No react-tinder-card callback-identity bugs found in SwipePage — handleDismissCancel uses setLocalResetTick correctly and guardConditions are stable useCallback refs. No stale useEffect dep array loops found — the only missing cleanup is the ArchitectProfilePage listProjects effect (low severity). The dominant finding class is i18n violations: strings introduced after the 2026-07-14 convention (fa36cd5) that bypass t(). Most severe are UploadWorkPage (entire file), SwipePage ConfidenceBar (post-convention commit db68d08 on 2026-07-17), and LLMSearchPage response templates. LoginPage.jsx and settings/* pages are fully i18n-compliant. UserProfilePage has a naming collision (timeout var named `t` shadows useTranslation's `t`) but is not a runtime bug.

### front-components-design
20 findings reported covering all severity tiers. Additional violations found but grouped into the most severe representative per file to stay at 20: (a) PersonaReport.jsx also hardcodes #ec4899 gradient (lines 194, 223, 401) — same pattern as BioPersonaFlipCard; (b) BoardDetailPage.jsx has 6 onMouseEnter/Leave handlers and #ec4899 gradient (line 726) plus body transition in index.css (line 18) uses hardcoded 0.2s instead of var(--motion-fast/normal); (c) FirmProfileHero.jsx line 169 hardcodes #ec4899 onMouseEnter color and line 18 has gradient; (d) SwipePage.jsx lines 491/610 hardcode the gradient; (e) DiscoveryPage.jsx lines 533/635/713 hardcode the gradient; (f) LikedProjectsPage.jsx line 241 + LLMSearchPage.jsx line 841 and ArchitectProfilePage.jsx lines 391/495 all hardcode the #ec4899 gradient. Total additional gradient violations across pages beyond the 20 reported: ~12 files, ~20 additional occurrences. The forbidden library check (Tailwind/MUI/Bootstrap/emotion/styled-components) found no violations — package.json and imports are clean. The @react-spring/web dependency in package.json is installed but not imported anywhere in src/ — dead dependency. No dangerouslySetInnerHTML was found. The article.url XSS vector (finding #14) is currently dormant because the backend serializer excludes articles[] (Phase 18 deferred) but the component is live and will become active when Phase 18 ships without a frontend guard.

### drift-dead-code
All 312 i18n keys are perfectly mirrored between ko and en (verified via Node.js AST eval — zero gaps). All algorithm.md Production Value entries for the 39 tracked params match settings.py exactly (dpp_singularity_eps 1e-9 == 1e-09 is a display format difference only, not a value mismatch). No large commented-out code blocks (>20 lines of code-like comments) found in either backend apps or frontend JSX. No orphan backend modules: event_log, caches, perf_timing are all imported by production code. No prod URLs or real credentials found in web-testing — FRONTEND_URL/BACKEND_URL point to localhost only. The web-testing runner.py Steps 2-3 and several subsequent text selectors form a coherent block of dead test flow that will abort the runner at Step 2 on every run against the current frontend. The BoardSurpriseView backend endpoint remains wired in urls.py but its sole frontend caller (SurpriseBoardModal.jsx) is never rendered; the backend endpoint could be considered live infrastructure (other callers could be added), so it is not reported as a finding. No additional findings were dropped — all 11 real issues are reported.
