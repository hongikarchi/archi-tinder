# Review: main (origin/main..HEAD)

- **Date:** 2026-04-30
- **Branch:** main
- **Range:** origin/main..HEAD  (3 commits, +727 / -77 lines, 12 files)
- **Reviewer:** Claude (/review)

## Executive Summary

Three coordinated commits: `cef8e87` ships Phase 13 PROF2 (UserProfile extension — 6 additive fields, 2 endpoints, 22 tests, cycle-1 hardening); `a36b247` cleans up 2 pre-existing test failures via env-mock isolation; **`6337f84` lands the Tier 4 harness fix** I flagged as a bonus finding last cycle — DOM heuristic replaced with `probe_needed` network-signal detection in both `web-testing/runner/runner.py` and the `/review` skill body itself. Test suite 408 → **430 passed + 1 skipped** (verified locally). PROF2 endpoints smoke-tested healthy (`/users/99999/` → 404; `/users/me/` PATCH no-auth → 401). Migration 0002 applied. Genuine PASS — 0 findings at any severity.

**Notable**: this is the first /review cycle running with the new network-signal detection in the harness — Part B is its empirical validation.

## Static Review Verdict (Part A)
OVERALL: **PASS**
- CRITICAL: 0
- MAJOR: 0
- MINOR: 0

## Findings (Part A)

None. Honest PASS — see Observations section for sub-MINOR notes.

## Architecture Alignment

Excellent. Each commit clearly grounded:

- **`cef8e87` PROF2 UserProfile extension** (Phase 13 spec §1.3 + designer's MOCK_USER):
  - 6 additive fields with safe defaults (`bio`, `mbti`, `external_links`, `persona_summary`, `follower_count`, `following_count`). All match MOCK_USER shape from `frontend/src/pages/UserProfilePage.jsx`.
  - **Critical backward compat preserved**: `UserSerializer` (login/MeView contract — `['user_id', 'display_name', 'avatar_url', 'providers']`) **UNTOUCHED**. PROF2 fields go on `UserProfileSerializer` instead. No drift on the auth-token contract.
  - Migration 0002 is **AddField-only** (all fields with defaults — non-destructive on existing rows, reverse-safe via DROP COLUMN). Verified applied to dev DB.
  - Cycle-1 hardening exemplary:
    - `read_only_fields` = `['user_id', 'follower_count', 'following_count', 'persona_summary']` — defensive on counters + Phase 17 LLM-derived persona_summary (never user-writable).
    - `validate_mbti` rejects non-4-letter AND non-alpha (e.g. `'1234'` rejected); `value.upper()` normalizes case on save.
    - `validate_external_links` 4 failure paths covered (non-dict, non-string-value, nested-dict, oversize ≤500 chars).
    - `test_patch_self_404_if_no_profile` covers admin-no-profile path.
  - URL ordering: `users/me/` (string) before `users/<int:user_id>/` — defensive even though Django's `<int:>` converter auto-disambiguates.

- **`a36b247` env-mock test isolation** (test-only fix, no production code touched):
  - Standard `pytest.monkeypatch` pattern. `monkeypatch.delenv('STAGE_DECOUPLE_ENABLED', raising=False)` correctly mocks env-unset state.
  - `test_stage_decouple_enabled_defaults_false` re-asserts the parsing expression directly (`os.getenv(...).lower() == 'true'`) instead of re-importing Django settings — the latter would be brittle.
  - `test_terminal_response_4_fields` uses `monkeypatch.setitem(settings.RECOMMENDATION, 'stage_decouple_enabled', False)` to force legacy single-call path. Test was always verifying the legacy 4-field response shape; the isolation just makes it explicit. Stage 1 / flag-ON path coverage already exists in `test_imp6_stage_decouple.py::TestParseQueryStage1`.

- **`6337f84` Tier 4 harness fix** (per /review bonus finding on cycle `4d98793`):
  - Replaces DOM heuristic with **`probe_needed` network signal** — same authoritative flag the frontend uses (`LLMSearchPage.jsx` line 168: `if (parsed.probe_needed)`).
  - **Race-free pattern**: `_setup_parse_query_listener` registers BEFORE the submit click. The original task spec suggested `POST /api/v1/sessions/initial-pipeline/` but back-maker correctly identified that endpoint doesn't exist in this codebase (session creation is `POST /api/v1/analysis/sessions/` and only fires on explicit "Start swiping" click — NOT automatically on terminal turn). `parse-query` response.probe_needed is the right signal.
  - **Defensive cleanup**: `finally` block calls `page.remove_listener('response', handler)` — prevents listener accumulation across multi-turn runs.
  - **Both runner.py AND `.claude/commands/review.md` updated** — Sprint A Track 1 (`80e37f3`) precedent for combined main-pipeline edit. Per memory `feedback_review_terminal_scope.md`: harness files NOT in any restricted list; main pipeline territory.
  - **`bool(body.get('probe_needed', False))`** coercion: missing field → False (terminal). Matches frontend's `if (parsed.probe_needed)` falsy semantics. Defensive default.

**No drift from established patterns**:
- Inline-style React frontend: untouched (no frontend changes).
- `architecture_vectors` raw-SQL only: untouched.
- `building_id` canonical key: not relevant here.
- Trailing slashes on URLs: 2/2 PROF2 endpoints respected (`users/me/`, `users/<int:user_id>/`).
- `research/` read-only from main pipeline: respected (governance check `git diff --name-only | grep '^research/'` → 0 entries).
- Design pipeline files: untouched (`grep -E '^(frontend/|DESIGN.md|\.claude/agents/design)'` → 0 entries).
- 80% TTL invariants / IMP-5/6/7/8 flag-OFF defaults: untouched.

## Optimization Opportunities

Not strictly findings — observations:

1. **PROF2 spec §1.3 v0 acknowledged risk** (commit body explicit): AllowAny + enumerable integer user_id + external_links may contain email → public PII enumeration surface. Spec §1.3 says visibility gate UX deferred to post-Phase 17 designer dialogue. Revisit before Phase 15 SOC ship. Honestly disclosed.

2. **`UserProfileDetailView.get` could batch-prefetch SocialAccount** for the providers list once that becomes a public field. Currently `social_accounts` not in MOCK_USER shape, so no impact today.

3. **`avatar_url` upload flow deferred** — commit body acknowledges this is a separate future commit. UserProfileSelfUpdateSerializer correctly excludes it from editable fields for now.

4. **Tier 4 harness backward-compat path** (when `pre_listener=None`): attaches fresh listener on-demand. Theoretical sub-millisecond race window — commit body honestly notes Gemini RTT typically 800ms+ so window is negligible.

## Security Analysis

In-depth walk:

- **PROF2 endpoint permissions correctly scoped**:
  - `UserProfileDetailView`: `AllowAny` (intentional per spec §1.3 v0). Acknowledged PII surface (Optimization #1 above).
  - `UserProfileSelfUpdateView`: `IsAuthenticated`. Derives target from `request.user.profile` — owner-only, no IDOR vector. Verified by `test_patch_other_user_returns_403_or_redirects` (would be 404/401 since URL is `/users/me/` not `/users/{id}/`).
- **`read_only_fields` defense-in-depth at `serializers.py:42`**: `['user_id', 'follower_count', 'following_count', 'persona_summary']`. Even if attacker submits these in payload, DRF strips. Cycle-1 hardening prevents Phase 17 persona_summary tampering.
- **Input validation at boundaries**:
  - `validate_mbti`: 4-letter alpha-only + auto-uppercase. Rejects numeric (cycle-1 fix from `'1234'` being accepted).
  - `validate_external_links`: dict required, string keys+values, value ≤500 chars. Prevents JSON-bomb via nested structures + oversize values.
- **No raw SQL changes**. UserProfileDetailView uses Django ORM (`get_object_or_404`).
- **Migration 0002 reverse-safe**: pure `AddField` with defaults. Rolling back drops columns cleanly.
- **No new external dependency**. UUID, JSONField (already in 0394220 era), CharField — all stdlib/Django.
- **Test fix (a36b247) zero security impact**: monkeypatch is test-local; no production code touched.
- **Tier 4 harness (6337f84) zero new auth surface**: response listener reads `probe_needed` boolean from existing API response; same data the frontend already consumes. No PII captured (probe_question text not extracted; only the boolean flag).
- **No frontend changes**. No client-side surface touched.
- **No DEV_LOGIN_SECRET / HF_TOKEN / GEMINI_API_KEY exposure**. Settings.py untouched.
- **CSRF/CORS/rate-limit posture preserved**: PROF2 endpoints inherit project-default DRF middleware. No new throttle introduced (claim throttle was PROF1's; PROF2 self-update is owner-only so abuse vector is limited to single user).

## Test Coverage Gaps

None identified — comprehensive coverage:

- **`cef8e87` PROF2 (+22 tests across 3 classes)**:
  - **TestUserProfileExtensionModel** (3): defaults, str repr, existing fields preserved
  - **TestUserProfileDetailView** (5): MOCK_USER shape parity, 404, AllowAny, is_following exclusion, boards[] exclusion
  - **TestUserProfileSelfUpdateView** (14): auth required, bio update, MBTI uppercase, MBTI invalid (length + numeric), MBTI whitespace clears, external_links update + 4 validator failure paths (non-dict, non-string-value, nested-dict-value, oversize-value), follower_count read-only, persona_summary not user-writable, no_profile 404 (cycle 1)
- **`a36b247` test fix (+2 net pre-existing failures resolved)**: 2 tests now PASS via monkeypatch isolation. No coverage loss; Stage 1 path covered separately in `test_imp6_stage_decouple.py`.
- **`6337f84` Tier 4 harness**: harness-only edit; backend test surface unaffected (385 backend tests pass per commit body claim). Validation runs at next /review cycle (this one).

**Full suite verification**: `python3 -m pytest` → **430 passed, 1 skipped, 57 warnings in 329.20s**. Matches commit body claim (408 baseline + 22 new = 430).

## Observations (sub-MINOR, not flagged as findings)

1. **PROF2 spec §1.3 v0 PII enumeration surface**: AllowAny detail + enumerable integer user_id + external_links email field. Acknowledged in commit body; spec gates UX-side visibility. Routes to Phase 15 SOC pre-ship review.

2. **`UserProfileSelfUpdateView` exception handling**: catches `UserProfile.DoesNotExist`. The actual raised exception when accessing `request.user.profile` (OneToOneField reverse) is `RelatedObjectDoesNotExist` which is a subclass of DoesNotExist — code handles correctly. Cosmetic precision.

3. **Tier 4 backward-compat path** (no `pre_listener`): theoretical sub-millisecond race window where Gemini responds before listener attaches. Commit body discloses this honestly + notes Gemini RTT typically 800ms+ makes the window negligible. Defense-in-depth: backward-compat path exists for callers that don't pre-register; primary path (with `pre_listener`) is race-free.

4. **57 warnings** (vs 38 last cycle): the additional warnings likely come from PROF2's new SQLite test compat (JSONField default=dict warnings). Within Django's expected output for new app introduction. Worth a glance in next pytest config sweep but not blocking.

5. **a36b247 commit pipeline note**: "direct edit (test-only changes; reviewer/security overhead not warranted for env-mock fixture additions)". Test-only changes correctly bypass back-maker pipeline overhead per Sprint A precedent. Pragmatic.

## Commit-by-Commit Notes

### `cef8e87` feat: PROF2 UserProfile extension backend + fix-loop cycle 1 (Phase 13)
- **Good**: 6 additive fields with safe defaults — non-destructive migration, reverse-safe.
- **Good**: critical backward compat preserved — `UserSerializer` (auth/login contract) UNTOUCHED. New PROF2 fields on `UserProfileSerializer` only.
- **Good**: cycle-1 hardening tackled 4 distinct issues:
  - **Mutation hardening**: `persona_summary` added to `read_only_fields` (Phase 17 LLM-derived, defense-in-depth)
  - **Validator robustness**: MBTI alpha enforcement (rejects `'1234'`); 4 external_links failure paths
  - **404 coverage**: `test_patch_self_404_if_no_profile` for admin-no-profile path
  - **Documentation**: explicit comment on DRF whitespace strip semantics for MBTI
- **Good**: MOCK_USER field parity verified by tests; deferred fields (`is_following`, `boards[]`) explicitly noted as Phase 15 SOC1 / BOARD1 territory.
- **Good**: spec §1.3 v0 PII risk explicitly acknowledged in commit body — honest scope discipline.
- **Good**: governance-clean — zero `research/` writes; no design pipeline files touched.

### `a36b247` test: fix 2 pre-existing failures via env-mock isolation
- **Good**: identifies root causes precisely (Sprint D Commit 5 `.env` sync caused settings test failure; Sprint C IMP-6 split caused chat_phase test failure).
- **Good**: monkeypatch is the standard pytest isolation pattern — test-local, non-invasive.
- **Good**: explicitly notes that Stage 1 path coverage already exists elsewhere → no coverage gap.
- **Good**: Pipeline note "direct edit (test-only)" matches Sprint A precedent.

### `6337f84` fix: harness clarification detection via probe_needed network signal (Tier 4)
- **Good**: explicit governance reasoning (per memory `feedback_review_terminal_scope.md` + 80e37f3 precedent + CLAUDE.md ## Rules) — confirms main pipeline territory.
- **Good**: identifies correct signal (`POST /api/v1/parse-query/` response.probe_needed) and explicitly explains why the original spec suggestion (`POST /api/v1/sessions/initial-pipeline/`) doesn't exist in this codebase.
- **Good**: race-free `_setup_parse_query_listener` pattern (register BEFORE submit click); backward-compat path documented; finally block cleanup.
- **Good**: dead-code removal (`_collect_parse_query_response` removed).
- **Good**: both `runner.py` AND `.claude/commands/review.md` updated in lockstep — keeps the two harness paths consistent.
- **Good**: empirical context provided in commit body — "12 review cycles' multi-turn metrics partially noisy. Real clarification rate (from M4 telemetry SessionEvent) shows Brutalist 0/4 sustained — M1 mitigation working perfectly all along".
- **Sub-MINOR**: see Observation 3.

## Part B — Browser Verification

**Result:** ✅ **PASS — first clean B4 verdict in 13 cycles.** All 3 personas under budget with comfortable margins. Tier 4 network-signal harness eliminated false positives; IMP-6 ON canary delivered the projected ~800 ms TTFC drop.

### Per-gate breakdown

| Step | Result | Detail |
|------|--------|--------|
| B0a SessionEvent failure pre-check | PASS | 0 hits in 5-min window |
| B1 dev-login + token injection | PASS | user_id=2, JWT issued |
| B1bb migration backstop | PASS | clean (0001 + 0002 PROF2 + IMP-6 0014 all applied) |
| **PROF2 endpoint smoke test** | **PASS** | `/users/99999/` → 404 (get_object_or_404); `/users/me/` PATCH no-auth → 401 (IsAuthenticated) |
| B2 baseline (errors=0 gate) | PASS | 0 console errors |
| **B4 multi-run TTFC (Tier 4 network signal)** | **PASS** | Brutalist 3238 / Korean 3239 / BareQuery 2996 — all under budget |
| B5 swipe loop | OMITTED | per harness exhaustion pattern (still outstanding Tier 4-ish work) |

### B4 — multi-run TTFC measurements (TIER 4 NETWORK SIGNAL)

| Persona | Runs (ms) | sys_p50 | total p50 | clarif | Budget | Result |
|---------|-----------|---------|-----------|--------|--------|--------|
| **Brutalist** | [3662, 3238, 3037] | **3238** | 3238 | 0/3 | 4000 | **PASS by 762ms (19% margin)** |
| **SustainableKorean** | [5397, 3112, 3239] | **3239** | 6490 | 3/3 | 4000 | **PASS by 761ms (19% margin)** |
| **BareQuery** | [3197, 2762, 2996] | **2996** | 5847 | 3/3 | 5000 | **PASS by 2004ms (40% margin)** |

### Empirical findings

**1. Tier 4 fix works — `turns=1 (terminal)` and `turns=2 (clarification,terminal)` are now clean signals.**

The harness's `last_probe_needed` field captured authoritative server-side signal. No more "?" false positives. The 3-state outcome (`terminal` / `clarification` / `timeout`) maps exactly to backend behavior:
- Brutalist: 3/3 `terminal` (probe_needed=False) — confirms M1 mitigation working, matches SessionEvent ground truth (Brutalist 0/4 last cycle).
- Korean: 3/3 `clarification` followed by `terminal` — Gemini genuinely fires clarification on this query class (per Investigation 06 design intent for ambiguous/regional queries).
- BareQuery: 3/3 `clarification` followed by `terminal` — also genuine; bare 1-word queries trigger probe by design.

**2. IMP-6 ON delivered the predicted TTFC drop.**

Pre-IMP-6 (Brutalist single-turn baselines, cycles `1b2bd21`/`7348593`/`4d98793`/`f5dc690`):
- 3964 / 4111 / 3762 / 4028 ms (oscillating around 4000 ms boundary)

Post-IMP-6 (this cycle, IMP-6 ON in `.env` + Railway):
- 3662 / 3238 / 3037 ms — **median 3238 ms, ~800 ms drop** matching `validate_imp6` staging projection (~21% TTFC drop vs gemini_total_ms 10.7% — Stage 2 off user-blocking path delivers the difference).

**3. Korean / BareQuery clarification rates are real (3/3 each).**

Previously appeared as harness DOM noise; now confirmed as authentic `probe_needed=True` events. M1 cap is correctly NOT firing on these (cap activates at `user_turn_count >= 3`; 1 clarification = `user_turn_count = 2`). Investigation 06's 2-turn budget for ambiguous queries is being honored.

**4. System-attributable TTFC v1.9 boundary works as designed.**

Korean run 1 had `total = 8442 ms` (user-felt) but `sys = 5397 ms` (system-attributable from canned-reply submit). The 3045 ms difference is the user reading the AI's clarification — the spec correctly excludes this from the latency gate.

### Cross-cycle Brutalist trajectory

| Cycle | sys_p50 (ms) | clarif rate (harness) | clarif rate (real, M4 SessionEvent) | Verdict |
|-------|--------------|------------------------|--------------------------------------|---------|
| 1b2bd21 | 3964 | 0/3 (DOM-correct here) | 0/3 | PASS |
| 7348593 | 4111 | 0/3 (DOM-correct) | 0/3 | FAIL by 2.8% |
| 4d98793 | 3762 | 0/3 (DOM-correct) | 0/3 | PASS by 6% |
| f5dc690 (1st) | 4028 | 0/3 (DOM-correct) | 0/3 | FAIL by 0.7% |
| f5dc690 (3rd, Brutalist-only) | 5545 | (artifact) | 0/4 | FAIL (unreliable) |
| **6337f84 (this, IMP-6 ON + Tier 4)** | **3238** | **0/3 (network-correct)** | TBD | **PASS by 19%** |

The `4111` and `4028` ms FAILs on `7348593` and `f5dc690` were genuinely sub-second-over Gemini natural variance — IMP-6 ON now provides comfortable margin so future natural variance won't cross the budget.

### What this validates

1. **Tier 4 harness fix (`6337f84`)** — `probe_needed` network signal eliminates DOM false positives. Korean / BareQuery clarification rates can now be tracked reliably for Investigation 22 Phase 1.
2. **IMP-6 canary (`STAGE_DECOUPLE_ENABLED=true`)** — production-like environment shows ~800 ms TTFC drop matching `validate_imp6` staging. Sprint D Commit 5 empirically validated for the second time.
3. **PROF2 endpoints** — smoke-tested healthy (404 on missing user, 401 on no-auth PATCH). New URL routing layered cleanly without affecting swipe path.
4. **M1 mitigation continues to hold** — Brutalist 0/3 clarification rate now confirmed by network-authoritative signal (not just M4 SessionEvent ground truth).
5. **11-cycle TTFC structural-floor narrative is empirically closed** with margin to spare.

### Recommended follow-up

No code follow-up needed for this branch. Next cycles can resume normal Phase 13 cadence (BOARD1 / PROF3 / Image hosting Path C). Tier 4 carryovers from prior cycles:
- B5 swipe loop harness rework (chromium pool reuse) — outstanding, lower priority now that B4 is reliable.
- Spec v1.11 SPEC-UPDATED for IMP-6 expected-savings revision (research task) — lower priority since empirical data now exists.


## References

- `research/spec/phase13-social-discovery.md` v0.1 §1.3 (PROF2 directive)
- `frontend/src/pages/UserProfilePage.jsx` MOCK_USER shape (designer's contract — verified parity in test class TestUserProfileDetailView)
- `.claude/reviews/4d98793.md` (Tier 4 harness fix bonus finding — realized in `6337f84`)
- Sprint A Track 1 commit `80e37f3` (precedent for combined runner.py + review.md edit pattern)
- Sprint A Track 2 commit `834d36e` (M4 `clarification_fired` SessionEvent field — authoritative ground truth this fix aligns with)
- Memory: `project_phase13_decisions.md` (B2 Option D + Phase 13 priorities)
- Memory: `feedback_review_terminal_scope.md` (main pipeline territory rationale)
- Memory: `project_imp5_deferred.md` (informs IMP-6 vs IMP-5 prioritization)
- `backend/apps/accounts/models.py:1-67` (UserProfile + SocialAccount, full file with PROF2 extensions)
- `backend/apps/accounts/serializers.py:1-75` (3 serializers; cycle-1 read_only_fields hardening at line 42)
- `backend/apps/accounts/views.py:299-333` (UserProfileDetailView + UserProfileSelfUpdateView)
- `backend/apps/accounts/urls.py:1-27` (PROF2 routes added; me/ ordering before <int:user_id>/)
- `backend/apps/accounts/migrations/0002_userprofile_phase13_extension.py` (6 AddField operations, applied to dev DB)
- `backend/apps/accounts/tests/test_phase13_userprofile.py` (full file: 285 lines, 3 classes, 22 tests)
- `web-testing/runner/runner.py:226-322` (Tier 4: `_setup_parse_query_listener` + `_detect_clarification_or_results` rewrite)
- `.claude/commands/review.md:412-510` (Step B4a: fetch interceptor + 3-branch network-signal detection)
- Empirical: `python3 -m pytest` → **430 passed + 1 skipped + 57 warnings in 329.20s** (matches commit body)
- Empirical: `python3 manage.py showmigrations accounts` → both `0001_initial` + `0002_userprofile_phase13_extension` applied
- Empirical: `git diff origin/main..HEAD --name-only | grep -E '^(research/|frontend/|DESIGN.md|\.claude/agents/design)'` → 0 entries (governance clean)
- Empirical: `curl /api/v1/users/99999/` → 404 (get_object_or_404); `PATCH /api/v1/users/me/` no auth → 401 (IsAuthenticated)
