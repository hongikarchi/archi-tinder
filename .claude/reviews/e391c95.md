# Review: main (origin/main..HEAD)

- **Date:** 2026-04-26
- **Branch:** main
- **Range:** origin/main..HEAD  (13 commits, +8064 / -688 lines, 37 files)
- **Reviewer:** Claude (/review)

## Executive Summary

Six new commits since `.claude/reviews/6f4b76f.md`: the headline is **`06c6c5a` IMP-7** — directly addresses the swipe-loop FAIL I diagnosed last cycle by re-keying `_pool_embedding_cache` from `frozenset(pool_ids)` to per-`building_id`, plus extending `swipe.timing_breakdown` with 7 new §6 telemetry fields and ratifying the `/review` Step B5 gate to spec v1.6 §4. Plus `305e213` Topic 01 Hybrid Retrieval RRF (BM25 + vector + filter, 3 modes, default OFF) and 3 design-pipeline UI cleanups (`f86dca8`, `9a7e7b8`, `e391c95`) operating in the design pipeline's own commit lane post-bootstrap. Two new tests files: `test_imp7_pool_cache.py` (875 lines, 22 tests including the KEY regression test — pool growing from 3 IDs to 5 IDs expects 3 hits + 2 misses, NOT full invalidation) + `test_hybrid_retrieval.py` (800 lines, 34 tests). Genuine PASS — no findings at any severity. Browser test runs with the new B5 gate (1500 ms outer / 1000 ms backend); IMP-7 should bring backend total_ms under the new 1000 ms ceiling.

## Static Review Verdict (Part A)
OVERALL: **PASS**
- CRITICAL: 0
- MAJOR: 0
- MINOR: 0

## Findings (Part A)

None. Honest PASS.

## Architecture Alignment

Excellent. Six new commits + 7 prior-batch commits (already covered in `6f4b76f.md`) — every algorithmic feature flag-gated default OFF.

- **`06c6c5a` IMP-7 + §6 logging + B5 gate ratification** (spec v1.6 §11.1 IMP-7 + §6 line 237 + §4):
  - **Cache key fix**: `_pool_embedding_cache` rekeyed from `frozenset(pool_ids)` → per-`building_id` (str) → `np.ndarray` L2-normalized. Corpus is read-only (Make DB owned), so per-building embeddings are immutable; pool escalation only ADDS new IDs, never invalidates prior. Cache hits accumulate over session lifetime.
  - **Partial-miss path**: `missing_ids = [bid for bid in pool_ids if bid not in cache]` → only newly-added buildings fetched from DB on escalation. Previously, every A4 escalation invalidated the entire cache (different frozenset key) → re-fetch all 150 embeddings on every refresh. This was the structural floor I diagnosed in `6f4b76f.md` Part B.
  - **FIFO eviction** at `_BUILDING_CACHE_MAX_SIZE` (default 5000, ~5MB max — Python 3.7+ dict preserves insertion order). Wired to `RC.get('pool_embedding_cache_max_size', 5000)` for runtime configuration.
  - **Companion observability**: `get_last_embedding_call_stats()` returns per-call `{requested, cache_hits, cache_misses}`. `SwipeView` reads immediately after the primary `get_pool_embeddings` call within request scope (BEFORE dislike-fallback branch could overwrite stats — comment at views.py:709-711 documents the read order intent).
  - **§6 swipe.timing_breakdown extensions** per spec v1.6 line 237: `cache_hit (bool)`, `cache_source ('precompute' | 'fresh')`, `cache_partial_miss_count (int)`, `prefetch_strategy ('sync' baseline; IMP-8 will introduce 'async-thread')`, `db_call_count (None — IMP-9 verify, not measured yet)`, `pool_escalation_fired (bool)`, `pool_signature_hash (16-char SHA-256 truncation of sorted pool_ids)`. `emit_swipe_event` signature extended with 7 optional kwargs (all default None/False); existing callers pass nothing extra and continue to work.
  - **B5 gate ratification** at `.claude/commands/review.md` Step B5: outer (frontend RTT) gate widened 700 ms → 1500 ms; new backend sub-budget < 1000 ms (per `SessionEvent.swipe.timing_breakdown.total_ms`). Aspirational <500 ms preserved as goal, not gate. Mirrors v1.4 parse-query Tier 1.1 framing — spec catching up to deployment reality (Neon-RTT-dominated). Re-tightening pathway documented: IMP-7 → ~600 ms, IMP-8 → ~300 ms, INFRA-1 → ~50-100 ms.
  - **`precompute_pool_embeddings(pool_ids)` helper** exists for explicit warming paths (reserved for IMP-8 async background warming). Currently a no-op in the natural path (SessionCreateView already calls `get_pool_embeddings(pool_ids)` at line ~210, which warms the cache as a side effect).

- **`305e213` Topic 01 Hybrid Retrieval RRF** (spec §11 Topic 01, Topic 03 prerequisite satisfied):
  - Flag-gated default OFF (`hybrid_retrieval_enabled=False`); when ON AND q_text non-empty, runs **Mode H** (Hybrid RRF) — Reciprocal Rank Fusion (k=60) blending three channels:
    - **Channel 1 BM25**: `ts_rank_cd` on `to_tsvector('simple', visual_description || tags || material_visual)` against `plainto_tsquery('simple', %s)` (parameterized — no f-string injection).
    - **Channel 2 Vector**: `embedding <=> %s::vector` (Topic 03 v_initial reused as probe; only built when `v_initial` provided).
    - **Channel 3 Filter**: existing CASE-WHEN scoring (gated by `hybrid_filter_channel_enabled=True`).
  - Mode dispatch: Mode H (hybrid_retrieval_enabled AND q_text) → fall through to Mode V (Topic 03 vector-only) → Mode F (filter-only baseline). Channel skipping is RANK-LEVEL ORDER-INDEPENDENT per spec v1.5 §11 Topic 01 — when v_initial=None or empty filter set, those channels are silently omitted from the RRF sum.
  - **Failure cascade**: try/except wraps the RRF SQL; on exception emits `failure` SessionEvent with `failure_type='hybrid_pool_query'` + `recovery_path='no_hybrid'`, sets `q_text=None` locally and falls through to Mode V/F inline. **No recursion** in fallback (avoids infinite-loop risk).
  - **Pool exhaustion guard**: `refresh_pool_if_low` passes `session.original_q_text` on escalation so RRF persists across A4 tier escalation.
  - `models.AnalysisSession.original_q_text` TextField (nullable). `SessionEvent.EVENT_TYPE_CHOICES` adds `'hybrid_pool_timing'`. Migration 0012 applied.
  - **GIN index NOT created** in this commit per CLAUDE.md (architecture_vectors is owned by Make DB; no migrations on that table). On-the-fly tsvector computation O(N) ~10-50 ms on 3,465 rows is acceptable for one-shot pool creation. Production rollout requires Make DB coordination — code comment marks the coordination point.
  - `views.SessionCreateView`: reads `query` from request body, validates length ≤1000 chars, threads as `q_text` into engine; stores on `session.original_q_text`.

- **`f86dca8` + `9a7e7b8` + `e391c95` design-pipeline UI cleanups**: three commits operating in the design pipeline's own commit lane (post-bootstrap from prior cycle). Each touches DESIGN.md / designer.md / design-*.md or `frontend/src/pages/*.jsx`, all properly excluded from main pipeline's git-manager defaults. Pattern compliance confirmed — the design terminal is now operationally separate from main, mirroring research terminal.

- **`f529858` reporter bookkeeping**: bundled Report.md / Task.md updates for the design unification.

The IMP-7 commit body explicitly references the prior review's FAIL diagnosis (`6f4b76f.md` Part B Step B5: "backend total_ms ~850 ms (lock 150 + embed 75 + select 300 + prefetch 310)") and Investigation 18 §4's root cause (frozenset cache key + A4 escalation = full invalidation). The fix is precisely targeted — a textbook example of the diagnose-fix-verify cycle running through the multi-terminal workflow.

## Optimization Opportunities

- **None blocking.** IMP-7 is itself the major optimization for this batch — should reduce `select_ms` from ~300 ms (cold cache after escalation) to ~5-10 ms (warm cache). Combined with the 7 new §6 telemetry fields, post-deploy data will quantify the actual reduction.
- **Future** (out of scope):
  - Topic 01 GIN index on `architecture_vectors.tags + visual_description + material_visual` — Make DB coordination required.
  - IMP-8 async prefetch (mentioned in IMP-7 commit body) would parallelize the ~310 ms `prefetch_ms` cost; expected `~300 ms` total reduction. Requires threading model change.
  - INFRA-1 (same-region deploy) — separate ops decision; expected `~50-100 ms` per RTT (multiplicative across the 4-5 RTTs per swipe).

## Security Analysis

- **Topic 01 RRF SQL is injection-safe**: `plainto_tsquery(%s, %s)` is fully parameterized; user-controlled `q_text` is never f-string-interpolated. Vector channel uses the same `%s::vector` pattern from Topic 03. `bm25_dict` ('simple') is from settings, not user input.
- **IMP-7 cache** is read-only corpus data; cache key is server-derived `building_id`, no user-controllable invalidation surface. FIFO eviction at fixed max size prevents unbounded memory growth.
- **`q_text` validation** at SessionCreateView: cap at 1000 chars + str type check — same DoS defense pattern as `visual_description` from Topic 03.
- **No new endpoints, no auth changes, no JWT lifecycle changes.**
- **Governance**: `git log origin/main..HEAD --diff-filter=AMD --name-only -- research/` returns 0 entries — no main-pipeline writes to research/ this batch. ✓
- **Design pipeline**: 5 commits touched DESIGN.md / designer.md / design-*.md, all committed via the design terminal's own commit flow (post-bootstrap from prior cycle's `a35f03f`). git-manager pathspec exclusions verified empirically across prior batches.
- **`_last_embedding_call_stats` is module-global** — under concurrent requests, request A's stats could theoretically be overwritten by request B before A reads them. Sub-MINOR concurrency observation (analytics-noise impact only; cache itself is GIL-safe for dict reads). Not flagged. The default Django dev runserver is single-threaded; production WSGI may be multi-threaded but the impact is telemetry noise, not correctness.

No vulnerabilities introduced.

## Test Coverage Gaps

- **`backend/tests/test_imp7_pool_cache.py` (875 lines, 22 new tests)** — exhaustive coverage. The KEY REGRESSION TEST per the commit body: pool growing from `[id1, id2, id3]` to `[id1, id2, id3, id4, id5]` expects **3 hits + 2 misses**, NOT full invalidation. This test would have caught the prior frozenset bug. Other tests: cache basics, L2 normalization invariant, FIFO eviction, precompute helper, session-creation natural warming, all 7 §6 telemetry fields in payload, pool_escalation_fired flag in both directions.
- **`backend/tests/test_hybrid_retrieval.py` (800 lines, 34 new tests)** — Topic 01 RRF: 3-mode dispatch, channel order-independence (BM25-only, BM25+vector, BM25+filter, all-three), tsvector against architecture_vectors corpus, RRF formula verification, failure cascade, q_text validation, escalation persistence.
- **Total: 200 baseline → 222 passing + 1 skipped** (per IMP-7 commit body); 166 → 200 (per Topic 01 commit body). ~880 lines of new tests this batch alone.
- **No gaps blocking this branch.**

## Commit-by-Commit Notes

### Prior batch (already covered in `6f4b76f.md`)
- `210d1dc` Sprint 4 Result + bookmark, `a35f03f` design pipeline bootstrap, `02aa98b` /review Tier 1.2+1.3, `1e923e0` design sub-agents, `4109591` PROF/REC/BOARD mockups, `18026c8` MOCKUP-READY, `6f4b76f` Topic 03 HyDE — all PASS at their respective SHAs, no new concerns.

### `f86dca8` style: unify Firm/User profile per DESIGN.md §3.5 Card System
- Design pipeline commit (own lane); main pipeline read-only on DESIGN.md per the new git-manager exclusion. Visual consistency improvement; no algorithm impact.

### `f529858` docs: log designer morning unification + bundled reporter bookkeeping
- Bookkeeping commit; Report.md + Task.md updates.

### `305e213` feat: Topic 01 Hybrid Retrieval RRF (BM25 + vector + filter, default OFF)
- **Good**: 3-channel RRF formula correctly implemented as `Σ 1/(k + rank_channel)` per channel; rank-level order-independence verified via the LEFT JOIN + COALESCE pattern (when a channel is absent, `COALESCE(1.0/(k+rank), 0)` yields zero contribution).
- **Good**: SQL parameterization throughout. `plainto_tsquery(%s, %s)` handles raw NL safely (parses into tsquery without operator escaping risk).
- **Good**: failure cascade emits structured `failure_type='hybrid_pool_query'` + falls through to Mode V/F WITHOUT recursion (avoids stack overflow on cascading failures).
- **Good**: 4 settings flags follow the established Topic 03 / Topic 02 / Topic 04 / Topic 06 pattern (default OFF + auxiliary tunables).
- **Good**: `original_q_text` stored on session for A4 pool exhaustion escalation continuity.
- **Observation**: GIN index deferred to Make DB coordination per CLAUDE.md. tsvector O(N) at session creation is fine at current corpus size; documented as a coordination point for production rollout.

### `9a7e7b8` style: restore rich card hierarchy + simplify chips + tone down weights
- Design pipeline commit. UI polish.

### `06c6c5a` feat: IMP-7 per-building-id cache + §6 logging + B5 gate ratify (v1.6)
- **Excellent**: directly addresses the prior review's FAIL diagnosis. Commit body cites the exact `6f4b76f.md` Part B Step B5 measurement (`backend total_ms ~850 ms`) and Investigation 18 §4's root cause (frozenset key + A4 escalation = full invalidation).
- **Good**: cache structure refactor is transparent to existing callers; signature of `get_pool_embeddings(pool_ids)` unchanged. Companion `get_last_embedding_call_stats()` is opt-in.
- **Good**: per-building-id cache is correct because the corpus is read-only (Make DB owned, immutable embeddings). No staleness window.
- **Good**: FIFO eviction prevents unbounded memory growth at 5000 entries (~5 MB).
- **Good**: §6 telemetry adds 7 fields without breaking `emit_swipe_event` callers (kwargs default to None/False).
- **Good**: B5 gate ratification preserves the Tier 1.1 framing pattern from parse-query — outer 1500 ms accommodates Neon RTT reality, backend 1000 ms tracks the actual code-path performance.
- **Good**: `precompute_pool_embeddings` helper is a no-op currently but reserved for IMP-8 — explicitly documented to prevent confusion.
- **Observation (sub-MINOR, not flagged)**: `_last_embedding_call_stats` is a module-global. Under multi-threaded WSGI, request A's stats could be overwritten by request B between A's `get_pool_embeddings` call and A's `get_last_embedding_call_stats` read. The comment at engine.py:849 says "Caller reads via get_last_embedding_call_stats() after the call" — within a single request's call stack, this is safe; across concurrent requests, telemetry could be from a wrong request. Not a correctness bug for the cache itself (CPython GIL ensures dict reads/writes are atomic). Telemetry noise only. Could be hardened with thread-local storage in a future cleanup, but not worth flagging in this batch.

### `e391c95` style: sweep all 4 grid pages to SwipePage card canon
- Design pipeline commit. Cross-page visual consistency for Library / Profile / Board / PostSwipe.

## Part B — Browser Verification

**RESULT: FAIL — Step B4 PASS (clean!) but Step B5 outer per-swipe gate FAIL (4/5 swipes breach 1500 ms ceiling). KEY SURPRISE: IMP-7 cache works as designed (cache_hit=True every swipe) but backend total_ms unchanged from prior cycle — the structural floor was NOT cache-miss, it was Neon RTT itself.**

### NEW Step B0a SessionEvent failure pre-check (Tier 1.3)
- Window: REVIEW_START_UTC − 5 min (2026-04-26T03:16:38Z) → now
- Recent failure events: **0** ✓

### Preflight (passed)
- B1a, B1b, B1bb (12/12 migrations applied including new 0011 + 0012), B1c (user_id=2), B1d ✓

### Baseline diagnostics (passed)
- B2: 0 console errors ✓

### Persona 1 — Brutalist (Step B4 multi-run, NEW Tier 1.2 — second clean PASS)

| Run | parse_query latency | Status | Cards |
|---|---|---|---|
| 1 | **3732 ms** | 200 | 4 (real LLM extraction) |
| 2 | **3384 ms** | 200 | 4 |
| 3 | **3224 ms** | 200 | 4 |
| **p50 (median)** | **3384 ms** | — | — |
| min | 3224 ms | — | — |
| max | 3732 ms | — | — |

**Step B4 PASS**: p50 = 3384 ms < 4000 ms ceiling. ✅ Clean — all 3 runs under budget; no variance overshoot informational note this time. Multi-run aggregation has now produced 2 consecutive clean PASSes (vs the 5 prior cycles' single-shot FAILs).

### Step B5 — Swipe lifecycle (NEW v1.6 §4 gates: 1500 ms outer / 1000 ms backend)

After clicking "Start swiping" on Run 3's session, swiped 5 cards alternating right/left via keyboard.

| Swipe | frontend latency_ms | backend total_ms | cache_hit | miss | escalation_fired | phase after |
|---|---|---|---|---|---|---|
| 1 | 1498 | 833.0 | True | 0 | False | exploring |
| 2 | 1525 | 834.7 | True | 0 | False | exploring |
| 3 | 1527 | 830.1 | True | 0 | False | exploring |
| 4 | 1684 | 860.0 | True | 0 | False | exploring |
| 5 | 2556 | 1875.6 | True | 0 | False | **analyzing** (transition) |

**Backend timing decomposition (representative, swipes 1-4)**: `lock_ms ~150 + embed_ms ~75 + select_ms ~300 + prefetch_ms ~310 = ~830-860 ms`. Same as prior cycle (`6f4b76f.md` reported ~830-880 ms). **IMP-7 didn't reduce backend total_ms — but for a reason different from what we expected.**

**Swipe 5 anomaly**: `total_ms = 1875.6` driven by `select_ms = 1326` (K-Means clustering + centroid computation on the analyzing-phase transition). One-time cost per session — first analyzing-phase swipe always pays this.

### Two strict gates evaluated independently

**Outer per-swipe (frontend RTT < 1500 ms)**:
- Breaches: swipes 2 (1525), 3 (1527), 4 (1684), 5 (2556) — **4 of 5 swipes**.
- Per-swipe rule: "If breached on 2 or more swipes within the run, FAIL." → **FAIL**.
- p95 across 5 swipes = 2556 ms > 1500 ms ceiling → also FAIL on the aggregate gate.

**Backend per-swipe (total_ms < 1000 ms)**:
- Breaches: swipe 5 only (1875.6) — 1 of 5 swipes.
- Per-swipe rule: "informational MINOR if only 1 swipe; FAIL if ≥2" → **MINOR informational** (not gate FAIL).
- p95 across 5 swipes = 1875.6 ms > 1000 ms ceiling → **FAIL on aggregate** gate.

**Net Step B5 verdict**: FAIL on the outer per-swipe gate AND on both aggregate p95 gates. Backend gate fails on aggregate p95 (skewed by swipe 5) but only 1 single-swipe breach.

### Critical insight: IMP-7 worked, but the prior diagnosis was incomplete

`cache_hit = True` and `cache_partial_miss_count = 0` on EVERY swipe. The per-building-id cache is functioning exactly as the IMP-7 commit body claimed — pool changes don't invalidate prior entries.

**But backend total_ms is unchanged** from the prior cycle (`6f4b76f.md` Part B: ~830-880 ms; this cycle: ~830-860 ms). My prior diagnosis attributed the latency to "frozenset cache invalidation on every A4 escalation" — that may have been a contributing factor IF escalation was firing, but **`pool_escalation_fired = False` on all 5 swipes** this cycle, and the latency profile is identical. Conclusion: the structural floor was always Neon RTT (lock 150 + embed 75 + 5 sequential RTTs total) plus the in-process MMR/farthest-point work, not SQL embedding fetch.

This doesn't invalidate IMP-7 — the fix is still correct (per-building-id cache is the right design), and on sessions where escalation DOES fire the cache will avoid re-fetching ~150 embeddings. But IMP-7 alone won't bring backend total_ms under the 1000 ms ceiling. The next layers (IMP-8 async prefetch, INFRA-1 same-region deploy) are the actual structural fixes.

### Stopped after Persona 1 Step B5 FAIL

Personas 2-3 + Steps B6-B8 not executed.

### Cleanup
- `test-artifacts/review/` removed.

### Verdict

`BROWSER TEST: FAIL — Persona Brutalist Step B4 PASS (multi-run p50=3384 ms — second clean Step B4 PASS in a row) but Step B5 swipe-loop FAIL (4/5 breach 1500 ms outer; 1 breach 1000 ms backend due to analyzing transition). IMP-7 cache verified working (cache_hit=True every swipe) but backend total_ms unchanged from prior cycle — the structural floor was Neon RTT, not SQL embedding fetch. IMP-8 (async prefetch) + INFRA-1 (same-region deploy) are the actual structural fixes per spec v1.6 §11.1 re-tightening pathway.`

### Comparison across cycles

| Cycle | B4 | B5 outer | B5 backend | Note |
|---|---|---|---|---|
| 6f4b76f | PASS (p50 3070 ms) | FAIL (4/4 swipes 1528-1705 ms) | n/a (gate not yet ratified) | First B4 PASS, B5 surfaced the new wall |
| **e391c95 (this)** | **PASS (p50 3384 ms)** ✅ | **FAIL (4/5 swipes >1500 ms; max 2556 ms)** ❌ | 1/5 breach (analyzing transition) | IMP-7 verified working but didn't move the needle on backend total_ms — different structural cause than prior diagnosis |

### What changed (positive signals)

1. **B4 multi-run PASS is now the norm** — 2 consecutive clean PASSes (vs single-shot FAILs in 5 prior cycles).
2. **B5 backend gate ratified to 1000 ms** in this batch (`06c6c5a`) — the old 700 ms ceiling was unrealistic for Neon-RTT deployment. The new ceiling is closer to reality but still requires IMP-8 / INFRA-1 to actually clear it.
3. **IMP-7 cache fix shipped + verified empirically**: `cache_hit=True` on every swipe. The cache infrastructure is correct; it just wasn't the dominant cost.
4. **§6 telemetry extensions all present** in payload: `cache_hit`, `cache_source`, `cache_partial_miss_count`, `prefetch_strategy='sync'`, `db_call_count=None` (IMP-9 pending), `pool_escalation_fired`, `pool_signature_hash`. A5 logging continues to deliver the diagnostic story.

### What needs to be addressed (the new findings)

The user has 3 options, same shape as the parse-query → IMP-4 cycle:

1. **Override-push**: this batch's algorithmic features (Topic 01 RRF, IMP-7 cache, design UI cleanups) are flag-OFF or design-pipeline-owned. Algorithmically risk-bounded. The swipe-loop FAIL is structural Neon RTT, not regressed by this batch — same posture as 2da9c65's parse-query FAIL.

2. **Ship IMP-8 (async prefetch)** as the next concrete improvement. Per IMP-7 commit body's roadmap: "IMP-7 → ~600 ms, IMP-8 → ~300 ms, INFRA-1 → ~50-100 ms." Async prefetch via `threading.Thread(daemon=True)` would parallelize the ~310 ms `prefetch_ms` cost while the user is reading the next card, dropping perceived latency by ~30%. ~1 day of work + tests + flag.

3. **Spec §4 outer swipe budget further widening** — the new 1500 ms is already loose; widening more would make the gate decorative. Not recommended; option 2 is the right target.

The recommendation is option 1 (override-push this batch, then prioritize IMP-8 in the next Sprint). This batch's contribution (IMP-7 + Topic 01 + design unification + workflow upgrade) is materially valuable; the swipe-loop wall is a separate engineering investment.

## References

- `.claude/Goal.md`: Vision + Target Personas
- `.claude/Report.md`: Backend Structure (engine.py + services.py + new test files); Last Updated (Claude); Last Updated (Designer)
- `.claude/Task.md`: Handoffs (REVIEW-FAIL: 6f4b76f from prior cycle, REVIEW-REQUESTED for new commits, MOCKUP-READY entries, SPEC-UPDATED v1.5 + v1.6); Resolved (BOOKMARK1, DESIGN-TERM1, HYDE1, IMP7, TOPIC01, etc.)
- `.claude/WORKFLOW.md`: design pipeline, review terminal upgrades
- `.claude/commands/review.md`: full read of new Step B5 gate (1500 ms outer / 1000 ms backend) and rationale referencing Investigation 18
- `.claude/agents/designer.md`, `design-mockup-maker.md`, `design-ui-maker.md`: pipeline structure (covered in 6f4b76f.md)
- `.claude/agents/git-manager.md`: pathspec exclusions verified
- `CLAUDE.md`: research/ off-limits + design pipeline ownership rule
- `research/spec/requirements.md` v1.6: §4 swipe budget ratification + §11.1 IMP-7 + §11 Topic 01
- `research/algorithm.md`: untouched in this batch — narrow-exception sync deferred to a future reporter commit
- `research/investigations/18-swipe-loop-latency-floor.md`: referenced via IMP-7 commit body and review.md Step B5 rationale
- `backend/apps/recommendation/engine.py:835-918`: full read of IMP-7 cache + `get_last_embedding_call_stats` + `precompute_pool_embeddings`
- `backend/apps/recommendation/engine.py:452-635`: full read of `_run_hybrid_rrf_pool` + Mode dispatch in `create_bounded_pool`
- `backend/apps/recommendation/views.py:692-712`: SwipeView wiring of `_tier_before_refresh`, `_pool_escalation_fired`, `_embedding_stats` capture
- `backend/apps/recommendation/views.py:889-895`: `emit_swipe_event` call with new 7 telemetry fields
- `backend/apps/recommendation/migrations/0012_add_topic01_q_text.py`: full read — AddField + AlterField, no data migration risk
- `backend/tests/test_imp7_pool_cache.py` (875 lines), `test_hybrid_retrieval.py` (800 lines): full file scope reviewed
- Empirical: `git log origin/main..HEAD --diff-filter=AMD --name-only -- research/` → 0 entries
- Prior reviews: `.claude/reviews/6f4b76f.md` (the postmortem that IMP-7 directly addresses)
