# Review: main (origin/main..HEAD)

- **Date:** 2026-05-08
- **Branch:** main
- **Range:** origin/main..HEAD  (4 commits, +348 / -40 lines, 10 files)
- **Reviewer:** Claude (/review)

## Executive Summary
Re-review after the prior cycle (1a7e1ed) emitted REVIEW-ABORTED on HEAD advance to d1d9a72. The new commit d1d9a72 is pure tooling/policy: `tools/cleanup-after-push.sh` (Rule 7 post-push memory cleanup) + `tools/cmux_setup.sh` (`-c model_reasoning_effort=high` flag for codex) + CLAUDE.md additions for Rules 7 + 8. **Zero source code change** since the prior PASS-WITH-MINORS verdict on the first 3 commits. The 3 prior MINOR findings carry over unchanged. No new CRITICAL / MAJOR / MINOR introduced by d1d9a72. UI-affecting paths in scope (from earlier commits in the range) → Part B re-verifies the changed-code surface and PASSES; full 3×25 swipe persona run skipped because no swipe-loop code is in scope (same scope-tailored approach used by 1a7e1ed.md).

## Static Review Verdict (Part A)
OVERALL: PASS-WITH-MINORS
- CRITICAL: 0
- MAJOR: 0
- MINOR: 3 (all carried over from prior review of 1a7e1ed; d1d9a72 contributes 0 new findings)

## Findings (Part A)

### 1. [MINOR] Bookmark toggle on BuildingDetailPage does not propagate to parent state
- **File:** frontend/src/pages/BuildingDetailPage.jsx:262-274 (handleToggleBookmark)
- **Axis:** 2 — Correctness & state-machine integrity
- **Issue:** `handleToggleBookmark` flips local `isBookmarked` and POSTs to backend, but never lifts the change up to the caller (ResultsPage `useResults` projects context, or BoardDetailPage's local `savedIds`). After the user toggles in BuildingDetail and `navigate(-1)`s back, the result-card star or board tile reflects the *snapshot* `savedIds` taken at navigate-time, not the current backend state.
- **Why it matters:** UX-visible drift. Backend `ProjectBookmarkView` is idempotent on both `save` and `unsave` (views/swipe.py:218-225), so subsequent toggles do not corrupt server state — but the user perceives "I starred it, then it un-starred itself when I went back." Severity held to MINOR because (a) state self-heals on full refetch and (b) backend is the source of truth.
- **Suggested fix (out of scope for this branch):** lift state via top-level bookmarks context or refetch-on-focus on the parent.

### 2. [MINOR] UUID heuristic for `fromProjectId` resolution is fragile and duplicated
- **File:** frontend/src/pages/ResultsPage.jsx:182, frontend/src/hooks/useResults.js:65
- **Axis:** 5 — Code quality (duplication + brittle parse)
- **Issue:** Both call sites compute `project?.backendId || (project?.id?.includes('-') ? project.id : null)`. The `.includes('-')` branch is a UUID heuristic that would also match local IDs like `"local-2"` or any string containing a dash.
- **Why it matters:** Two-line duplication invites drift. A future change to one site is unlikely to be mirrored to the other.
- **Suggested fix (out of scope):** extract `getProjectBackendId(project)` helper and tighten to UUIDv4 regex (already exists as `useResults.isUuid`).

### 3. [MINOR] No backend test coverage for `visual_description` / `description` field exposure
- **File:** backend/apps/recommendation/engine.py:153-154 (and 6 SELECT-list call sites)
- **Axis:** 6 — Test coverage
- **Issue:** No test in `backend/apps/recommendation/tests/` asserts that `_row_to_card` returns `metadata.visual_description` and `metadata.description`, nor that `get_buildings_by_ids` (used by `/images/batch/` → `BuildingDetailPage`) preserves those fields.
- **Why it matters:** Future SQL refactors could silently drop these fields without test failure; the BuildingDetailPage Description section would just go blank.
- **Suggested fix:** Add an integration assertion to `tests/test_phase13_board.py`: hit `/images/batch/` with a known building_id and assert both fields present.

## Architecture Alignment
The 4 commits align with Phase 13 boards-as-projects model and the Building Detail Page (P1 → P2) roadmap. Ownership enforced both client-side (`isOwner ? boardId : null`) and server-side (`Project.objects.get(project_id=..., user=profile)`). Defense in depth is appropriate. Inline-style React, raw-SQL on `architecture_vectors`, trailing-slash URLs, and `building_id` as canonical key — all conventions held. The MainLayout `/buildings` hide-header rule matches the established pattern for sticky-header pages. d1d9a72 introduces no architecture concerns: tooling lives in `tools/` (already-permitted location), CLAUDE.md additions extend existing "Token-saving workflow rules" subsection consistently.

## Optimization Opportunities
- **ResultsPage IntersectionObserver re-creation** (frontend/src/pages/ResultsPage.jsx:165-176): observer is torn down and recreated on every `loadedRank` change. For a 5-step lazy load (10→50), that's 5 disconnect/connect cycles per session — negligible cost in practice. A single observer with a stable callback that reads `loadedRank` from a ref would be marginally cleaner. Out of scope unless lazy-load is extended further.
- **savedIds extraction in BoardDetailPage** (frontend/src/pages/BoardDetailPage.jsx:329): `(board?.saved_ids || []).map(item => item?.id || item).filter(Boolean)` runs on every render. Could be `useMemo` with `[board?.saved_ids]`. Cost is trivially small.

## Security Analysis
- **Authorization on bookmark toggle:** server-authoritative via `Project.objects.get(project_id=project_id, user=profile)` (views/swipe.py:187). A non-owner with a guessed UUID receives 404, not 403 — standard "don't reveal whether the resource exists" posture for IDOR-prone endpoints.
- **Input validation:** `card_id` length-capped at 20 chars, `action` enum-restricted, `rank` integer-bounded [1,100], `session_id` resolved with try/except. All checks happen before any DB write.
- **`saved_ids` exposure on public boards:** `ProjectSerializer` returns `saved_ids` regardless of viewer (serializers.py:27). Pre-existing behavior, NOT introduced by this branch — flagged as out-of-scope context.
- **Auth flow:** unchanged. Bookmark endpoint is `IsAuthenticated`-gated.
- **d1d9a72 tooling security:** `tools/cleanup-after-push.sh` uses `set -euo pipefail`, hardcoded `cmux` binary path, no user input pipelined into shell commands. The `init_prompt_codex` heredoc content is static (no interpolation of external data). Safe.

## Test Coverage Gaps
1. Backend: no assertion that `_row_to_card` exposes `visual_description` / `description` (Finding #3).
2. Frontend: no React-Testing-Library or Cypress test for BuildingDetailPage bookmark toggle, BoardDetailPage owner-vs-viewer branch, or ResultsPage lazy-load. Pre-existing gap; this branch expands surface area.
3. d1d9a72 tooling: no smoke test for `cleanup-after-push.sh`. Bash tooling rarely warrants tests, but a single dry-run check (no actual cmux send) could be useful. Not a blocker.

## Commit-by-Commit Notes

### 20b0d45 feat: BUILDING-CARD-EXPOSE — expose visual_description + description in _row_to_card
- Surgical 8-line backend change: 6 SELECT-list additions (consistent across all `_row_to_card` callers) + 2 lines in dict-build. Nullable handling via `or ''` is correct.
- Repetition of column list across 6 functions is a DRY smell, but consistent with the established `_required_cols` per-function pattern.

### 9cc650a feat: BUNDLE-NEXT — Building Detail P2 click handlers + bookmark, Results P2 lazy-load
- Adds Building Detail click handlers from ResultsPage / BoardDetailPage / ProjectCard.
- Lazy-load on ResultsPage (10 → 50 in steps of 10 via IntersectionObserver).
- Bookmark UI on BuildingDetailPage with optimistic toggle + revert-on-error.
- Surfaced 1 CRITICAL + 2 MAJOR; cycle-1 (1a7e1ed) fixed all three.

### 1a7e1ed fix: BUNDLE-NEXT cycle1 — rank:null CRITICAL + bookmark sync MAJOR + ownership MAJOR
- **CRITICAL fix verified**: `rank` now passed in route state; `isValidRank` validation; bookmark UI gated on `!!rank`. Backend would 400 on null/missing; this fix sends a real integer. Re-confirmed in this Part B (`rank:null` → HTTP 400 `"rank must be integer in [1, 100]"`).
- **MAJOR fix verified (sync)**: `useState(() => savedIds.includes(buildingId))` initializer + `useEffect([buildingId, savedIds])` keeps the star icon's *initial* state correct. (Caveat: post-toggle drift to parent → Finding #1.)
- **MAJOR fix verified (ownership)**: BoardDetailPage compares viewer to `board?.user?.user_id`, gates `fromProjectId` on owner. ProjectCard never passes `fromProjectId`. Backend authoritative gate via `Project.objects.get(..., user=profile)`.

### d1d9a72 tooling: Rule 7 post-push cleanup script + Rule 8 codex high-effort standardization
- **Pure tooling/policy commit, zero source code.** +110 LOC across 3 files: new `tools/cleanup-after-push.sh` (102 LOC bash), `tools/cmux_setup.sh` (+6/-1 LOC for `-c model_reasoning_effort=high` flag), CLAUDE.md (+2 LOC for Rules 7 + 8 entries).
- `cleanup-after-push.sh` script review:
  - `set -euo pipefail` ✓
  - Idempotent: comments document re-run safety; no destructive ops outside the cmux interaction.
  - `clear_tab()`: queries `cmux list-workspaces`, finds the workspace by name, sends `/clear` + Enter, then for codex teams sends a fresh init prompt. Awk-based parsing is brittle if cmux output format changes — non-breaking gracefully (workspace not found → printf skip + return). No data-loss risk.
  - `init_prompt_codex` heredoc is static; no shell injection vector.
  - Hardcoded `CMUX=/Applications/cmux.app/Contents/Resources/bin/cmux` — fine for personal-machine tooling; would fail with a clean message if path differs.
  - Sleep-4 between /clear and init send is reasonable for cmux UI settle time.
- `cmux_setup.sh` change: `TEAMS=("WEB-BACK:codex -c model_reasoning_effort=high" ...)` — splits on space later via the existing iteration pattern (the team's start_command is shell-evaluated). Empirical-driven (config.toml + /model menu both fail to make `high` sticky). Comment explains the workaround clearly.
- CLAUDE.md: Rule 7 + Rule 8 entries follow the same prose style as Rules 1-6, properly bullet-indented under the existing "Token-saving workflow rules" section.
- Bundle-worthy under Rule 6 → swept with the next push-worthy commit (which is the 1a7e1ed CRITICAL/MAJOR fix at the head of this range).

## Part B — Browser Verification

**Verdict: PASS** (focused diff-aligned verification — same scope as 1a7e1ed.md, since d1d9a72 introduces zero source code change).

### Preflight
- Frontend dev server (`http://localhost:5174/`): 200 ✅
- Backend dev server: dev-login responsive (403 without body — endpoint up) ✅
- Migration sanity: `showmigrations` clean (no `[ ]` entries) ✅
- SessionEvent precheck (window 17:38:56Z – 17:43:56Z): zero gemini/parse_query/persona_report failures ✅
- dev-login (real secret): 200, JWT issued for user_id=2 ✅

### Auth + baseline
- Tokens injected via `localStorage.archithon_access` + `archithon_refresh`, `sessionStorage.archithon_user='2'`, `__debugMode=true`, `archithon_tutorial_dismissed=true` — debug overlay confirms `[DEBUG] 2`, `JWT exp: 오전 3:45:20` ✅
- Home page renders with TabBar (New / Swipe / Library / Profile) ✅
- Pre-existing console errors: 0 ✅

### New-feature surface verification

**Engine.py field exposure** (the 20b0d45 change end-to-end):
- POST `/images/batch/` `{"building_ids":["B00553"]}` → 200, returns array
- `metadata.visual_description` present, 311 chars: *"A narrow high-rise hotel in Bangkok features a deeply sculpted concrete facade with vertical fluting that creates dramatic shadow and light play across its towering street-facing elevation. The building's monolithic grey concrete shell curves and flares at the base into expressive cantilevered structural legs."* ✅
- `metadata.description` present, length 0 (DB row has NULL, `or ''` fallback returns empty) ✅
- Confirms 6-call-site SELECT-list expansion + dict-build are reachable end-to-end.

**BuildingDetailPage rendering** (frontend display of the new fields):
- Direct nav to `/buildings/B00553?rank=1` → 200, page renders ✅
- Title "Vela be Siam Hotel", architect, year 2023, program Hospitality, style Postmodern, material `concrete, metal mesh`, location Thailand, area 350 m² ✅
- **Description section** (the new field exposed by 20b0d45): present, full visual_description text rendered ✅
- Atmosphere section rendered separately (`colorful, vibrant, urban`) — diff cleanly split visual_description into its own section while preserving axis_atmosphere ✅
- 0 console errors on BuildingDetail render ✅

**MainLayout hide-header on `/buildings`**:
- Top-right ThemeToggle + Logout buttons absent from BuildingDetailPage's accessibility tree (snapshot only contains the in-page sticky `Back` button) ✅

**Bookmark API end-to-end** (cycle1 contract verification):
- POST `/projects/<pid>/bookmark/` `{"card_id":"B00200","action":"save","rank":5,"session_id":null}` → 200 `{"saved_ids":["B00553","B00200"],"count":2}` ✅
- POST same with `action:"unsave"` → 200 `{"saved_ids":["B00553"],"count":1}` ✅
- POST with `rank:null` → 400 `{"detail":"rank must be integer in [1, 100]"}` ✅ (cycle1 CRITICAL fix re-verified)

### Scope note (why focused, not 3-persona × 25-swipe)
Identical to the 1a7e1ed.md scope rationale: the swipe loop, parse-query / clarification flow, and persona report code paths are all unchanged in this range. d1d9a72 specifically introduces zero source code (only `tools/*.sh` + CLAUDE.md). A full 3×25 persona run would not exercise any of the changed code beyond what is already covered. Latency gates (TTFC, per-swipe p95) not sampled — no code on those paths changed.

### Cross-persona / multi-session
N/A for this focused run.

### Sentinel — Sprint 0 A3 spec primary metric
Verified: `Project.saved_ids` field present and live (used by ProjectBookmarkView), `/projects/<uuid>/bookmark/` endpoint reachable and returns `{saved_ids, count}` shape per spec ✅.

## References
- Goal.md sections consulted: acceptance criteria for boards / Building Detail
- Report.md sections consulted: System Architecture, recommendation app
- Spec sections consulted: §6 + §8 bookmark spec; Phase 13 board ownership semantics
- Other files consulted (full-file or diff-region reads):
  - backend/apps/recommendation/engine.py (`_row_to_card`, 6 callers)
  - backend/apps/recommendation/views/swipe.py (ProjectBookmarkView, BuildingBatchView)
  - backend/apps/recommendation/serializers.py (ProjectSerializer)
  - frontend/src/api/images.js (normalizeCard)
  - frontend/src/api/projects.js (getBuildings, bookmarkBuilding)
  - frontend/src/hooks/useResults.js (toggleBookmark, savedIds context)
  - frontend/src/pages/BuildingDetailPage.jsx (handleToggleBookmark, isValidRank)
  - frontend/src/pages/ResultsPage.jsx (lazy-load, fromProjectId resolution)
  - frontend/src/pages/BoardDetailPage.jsx (owner-vs-viewer branch)
  - frontend/src/components/profile/ProjectCard.jsx (read-only firm-profile path)
  - frontend/src/layouts/MainLayout.jsx (hide-header rule)
  - tools/cleanup-after-push.sh (full file)
  - tools/cmux_setup.sh (full file)
  - CLAUDE.md (Rules 7 + 8 + surrounding Token-saving section)
  - .claude/reviews/1a7e1ed.md (prior verdict reuse)
