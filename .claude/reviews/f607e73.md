# Review: main (origin/main..HEAD)

- **Date:** 2026-04-25
- **Branch:** main
- **Range:** origin/main..HEAD  (13 commits, +1452 / -747 lines, 27 files)
- **Reviewer:** Claude (/review)

## Executive Summary

Three new commits since `.claude/reviews/5d85b90.md` close out the prior `REVIEW-FAIL: 5d85b90` Part A MINOR and the v1.1 spec IMP-1 directive: `2f8a943` fixes orchestrator Step 2.5's app-filter inconsistency (the exact MINOR I flagged), `a9305e4` corrects the long-standing `farthest_point_from_pool` max-max → max-min bug AND vectorizes via NumPy BLAS matmul (~20-50× speedup), and `f607e73` is the reporter narrow-exception sync. The IMP-1 commit is exemplary — implementer caught and corrected a geometric error in investigation 02's spec text (cos(Y,B)≈0.912 not 0.5 as the prose claimed) and substituted a correct fixture while preserving spec intent. Five new tests including a counterexample regression test that distinguishes pre-fix from post-fix. Genuine PASS, browser test (Part B) will run because UI-affecting paths in scope.

## Static Review Verdict (Part A)
OVERALL: **PASS**
- CRITICAL: 0
- MAJOR: 0
- MINOR: 0

## Findings (Part A)

None. Honest PASS.

## Architecture Alignment

Excellent across all 13 commits, with 3 new ones tightly aligned to the prior review's findings and spec v1.1:

- **`2f8a943`** (orchestrator Step 2.5 app-agnostic) — closes `5d85b90.md` Finding 1 with a 2-line fix. Drops the `recommendation` argument from `manage.py showmigrations` so the trigger condition ("migration in ANY app") and the verification command match. Now consistent with `commands/review.md` Step B1bb. Adds an explanatory note in the same diff so future maintainers don't reintroduce the narrowing.

- **`a9305e4`** (IMP-1: max-min correctness + NumPy vectorization) — implements spec v1.1 §11.1 IMP-1 per investigation 02. Two tightly-coupled changes bundled per investigation's recommendation (vectorizing a wrong algorithm just speeds up the wrongness):
  - **Correctness**: `farthest_point_from_pool` accumulator was inverted. Pre-fix code computed "for each candidate, max distance to its FARTHEST exposed item, pick max" (max-max); correct Gonzalez requires "for each candidate, max distance to its NEAREST exposed item, pick max" (max-min). Pre-fix silently picked near-duplicates of an exposed item if they were also far from something else.
  - **Vectorization**: Replaces nested Python loop (~7500 individual `np.dot` calls at typical N=150 × M=50) with single BLAS matmul `C @ E.T` plus `max(axis=1)` + `argmin`. Math: `sim[i][j]` = cos(cand_i, exp_j); `sim.max(axis=1)[i]` = nearest-exposed similarity for candidate i; `argmin` over that = candidate with farthest nearest-exposed. Verified against docstring claim.
  - **Contract preserved**: Signature unchanged. All 12 callers (10 views.py, 2 algorithm_tester.py) untouched. None / random fallbacks preserved. Defensive skip for missing embeddings preserved.
  - **Downstream impact**: Topic 11's "2-approximation bound" claim (research/search/11-cold-start-seed.md) and Section 4 C-3 Better layer 3 ("first 3-5 diverse seed cards") both depend on max-min behavior. Pre-fix, neither delivered diverse selection; post-fix, both do.
  - **Tests**: New `TestFarthestPointFromPool` class with 5 tests. The headline test `test_selects_max_min_diverse_candidate` uses a 3D fixture (A=(1,0,0), B=(0,1,0), X=unit(0.99,0.141,0), Y=unit(0.5,0.5,√0.5)) where pre-fix picks X (near-duplicate of A) and post-fix picks Y (genuinely diverse). 4 edge-case tests cover the defensive paths.

- **`f607e73`** (algorithm.md sync via narrow exception, third use) — reporter Step 6 compliance verified:
  - 6a — no Production Value column changes (a9305e4 didn't touch RECOMMENDATION dict).
  - 6b — single italic annotation under "Greedy Farthest-point Sampling" subsection.
  - 6c — Last Synced replaced (190c830 → a9305e4).
  - 6d — no theory rewrites, no new sections, no removed lines, no other research/ paths touched.
  - Commit body explicitly enumerates files INTENTIONALLY left out of this commit (research/search/, research/spec/, research/investigations/) per governance — clear chain-of-custody.

The fix-loop sequence is textbook: I flagged a MINOR in `5d85b90.md`, main fix-looped with `2f8a943`. Spec v1.1 added §11.1 IMP-1, main shipped `a9305e4`. Reporter synced algorithm.md per its narrow exception in `f607e73`. Three commits, three independent obligations, all closed cleanly.

## Optimization Opportunities

- **None blocking.** `a9305e4`'s vectorization itself IS the optimization for this batch.
- **Future**: Once investigation 01 O1 instrumentation lands, the per-swipe `select_ms` histograms will reveal whether the post-IMP-1 latency drop materializes as expected. If it does, the `swipe_latency` p95 budget pressure (which surfaced as the FAIL in `5d85b90.md`'s Part B) eases. The pre-IMP-1 nested loop ran once per swipe in exploring phase — the 20-50× speedup is a real backend latency improvement on the hot path.

## Security Analysis

- **No new endpoints.** No auth surface changes. No JWT/token lifecycle changes.
- **`a9305e4`** is a pure algorithmic correctness + perf change — no validation surface, no input-handling boundary moved.
- **`2f8a943`** widens an internal sanity check (orchestrator Step 2.5) — broader coverage, never narrower; defensive.
- **Governance verification**: `git log origin/main..HEAD --diff-filter=AMD --name-only -- research/` shows ONLY `research/algorithm.md` is touched, in only the two reporter-sync commits (`f607e73` and `88f0532`). No edits to `research/spec/`, `research/search/`, or `research/investigations/` from the main pipeline. ✓ Narrow exception is honored.
- **Math validation**: implementer caught and corrected a geometric error in investigation 02's spec text. Investigation 02 line 156 fixture had `Y=unit(0.5, 0.866, 0)` and `B=unit(0.1, 0.995, 0)`, which gives cos(Y,B) = 0.5·0.1 + 0.866·0.995 ≈ 0.912 — not 0.5 as the prose claimed. Implementer substituted a properly-equidistant 3D fixture. This is the kind of diligence that prevents a "test passes by coincidence" anti-pattern.

No vulnerabilities introduced.

## Test Coverage Gaps

- **Pool normalization (8bf73b8)**: 2 tests (`TestPoolScoreNormalization`).
- **Convergence fixes (3ee9c77)**: 2 tests (`TestConvergenceSignalIntegrity`).
- **Schema A3 (190c830)**: 6 tests (`TestProjectSchemaA3`).
- **IMP-1 (a9305e4)**: 5 tests (`TestFarthestPointFromPool`).
- **Total backend tests: 44 passing** (39 prior + 5 new).
- **No gaps that block this branch.**
- Out-of-scope future tests:
  - Multi-iteration Gonzalez convergence (the function is called once per round; multi-call dependencies could be tested for ordering invariants).
  - Performance regression test (benchmark p50/p95 of the function under N=150, M=50 fixture, asserting <5ms wall time).

## Commit-by-Commit Notes

### `354a2ef` docs: research/ strictly read-only for main + review terminals
- Already covered in `88f0532.md`. No new concerns.

### `8bf73b8` fix: normalize pool scores to [0,1] (Topic 12)
- Already covered in `88f0532.md`. PASS.

### `ad2cac0` docs: POOL1 resolved
- Bookkeeping. PASS.

### `f04646f` fix: max_consecutive_dislikes 10 → 5 (Section 5.1)
- Already covered in `88f0532.md`. PASS.

### `0e99ac6` docs: DISLIKE1 resolved
- Bookkeeping. PASS.

### `8f4eaae` feat: deep-web-tester + reporter narrow exception
- Now superseded by `6984993` unification but the reporter narrow-exception machinery survives. PASS.

### `190c830` feat: project schema migration (A3)
- Already covered in `88f0532.md`. PASS.

### `88f0532` docs: SCHEMA1 resolved + first algorithm.md sync
- Already covered in `88f0532.md`. PASS.

### `6984993` refactor: unify pre-push gate as `/review`
- Already covered in `5d85b90.md`. PASS — clean delete-and-replace.

### `5d85b90` fix: orchestrator pipeline migration-apply gap (3-tier guard)
- Already covered in `5d85b90.md`. PASS — Tier 1 (back-maker) + Tier 1.2 (orchestrator Step 2.5) + Tier 2 (review.md Step B1bb).

### `2f8a943` fix: orchestrator Step 2.5 showmigrations check is app-agnostic
- **Good**: closes `5d85b90.md` Finding 1 directly. Drops `recommendation` argument from `manage.py showmigrations` so the verification command matches its own trigger condition AND `commands/review.md` Step B1bb's check.
- **Good**: adds an explanatory parenthetical comment in the same diff so future readers see why the broader form is correct ("Step 2.5's trigger condition fires for migrations in ANY app under `backend/apps/*/migrations/`, so the check must also be app-agnostic. Matches `commands/review.md` Step B1bb's check.").
- **Good**: trivial 2-line change (one command edit + one note line). Surgical scope.

### `a9305e4` fix: farthest_point max-min correctness + NumPy vectorization (IMP-1)
- **Good**: bundles correctness + perf per investigation 02's recommendation. Vectorizing the wrong algorithm just makes the bug faster — they're inseparable.
- **Good**: docstring rewritten to accurately describe the algorithm (Gonzalez greedy farthest-point sampling, max-min interpretation explicitly stated). Pre-fix docstring described "farthest from all exposed" which was ambiguous and partly wrong.
- **Good**: math correctness verified. `C @ E.T` produces an (N, M) similarity matrix; `sim.max(axis=1)` is the per-candidate nearest-exposed similarity; `argmin` picks the candidate whose nearest-exposed is farthest = correct max-min.
- **Good**: edge cases preserved — None on no candidates, `random.choice` on no anchor (empty exposed OR all exposed missing from embeddings), defensive skip for missing embeddings (`bid in pool_embeddings` filter).
- **Good**: tie-breaking preserved (`np.argmin` returns first index on ties; pre-fix used `>` strict comparison so first wins; behavior identical).
- **Good**: signature unchanged → all 12 callers (10 views.py, 2 algorithm_tester.py) unaffected.
- **Excellent**: implementer **caught a geometric error in spec text** — investigation 02 line 156's fixture had Y=unit(0.5, 0.866, 0) and B=unit(0.1, 0.995, 0), which yields cos(Y,B)=0.912 (not 0.5 as the prose claimed). Implementer substituted a correct 3D orthogonal fixture (A=(1,0,0), B=(0,1,0), Y=unit(0.5, 0.5, √0.5)) and documented the substitution in the test docstring. This catches the kind of "test passes by coincidence" trap that erodes signal over time.
- **Good**: 5 new tests cover the headline regression + 4 edge cases.

### `f607e73` docs: IMP1 resolved + algorithm.md sync (third narrow-exception use)
- **Good**: reporter Step 6 strict compliance — 6a (no Production Value changes), 6b (one inline annotation under correct subsection), 6c (Last Synced replaced), 6d (no theory rewrites, no new sections, no removed lines, no other research/ paths). Verified by `git diff` showing only +3/-1 lines in `research/algorithm.md`.
- **Good**: commit body explicitly enumerates files left out per governance (research/search/11, research/spec/requirements.md research-terminal-pending v1.1, research/investigations/ contamination). Provides clear audit trail.
- **Good**: third successful use of the narrow exception (after 88f0532 and earlier syncs). The discipline scales.

## Part B — Browser Verification

**RESULT: FAIL (Step B4 latency gate — second consecutive run on same gate)**

### Preflight (passed)
- B1a artifact dir created ✓
- B1b dev server health: frontend 200 ✓; backend dev-login responsive ✓
- **B1bb migration sanity backstop: PASS** (no unapplied migrations) — the 3-tier guard (`5d85b90`) continues to function correctly.
- B1c dev-login: user_id=2, access_len=231 ✓
- B1d tokens injected, reload to authenticated home ✓

### Baseline diagnostics (passed)
- B2: 0 console errors. ✓

### Persona 1: Brutalist (Step B4 latency gate FAIL)
- Project name: "P1 Brutalist v3"; navigated `/` → `/new` → `/search`.
- Query submitted: `concrete brutalist museum`.
- Fetch interceptor installed before submit; `t_submit` captured.
- `POST /api/v1/parse-query/` returned **200 in 6706 ms**.

### Failure detail

**Strict gate (Step B4)**: `t_first_card - t_submit < 4000 ms` (non-bare query). parse-query alone consumed 6706 ms — **worse than prior `5d85b90.md` Part B run (5496 ms) and `88f0532.md` first attempt (3437 ms)**. The full time-to-first-card (parse-query + session creation + first-card image load) would land somewhere in 7000-8500 ms range. Hard ceiling is 4000 ms.

### Pattern analysis across 3 consecutive runs

| Run | parse-query latency | strict gate (4000 ms) |
|---|---|---|
| `88f0532` first attempt | 3437 ms | PASS (barely; structured filters extracted) |
| `5d85b90` (prior /review) | 5496 ms | FAIL |
| `f607e73` (this run) | 6706 ms | FAIL — worse |

Three consecutive runs on the same query, same backend code path. parse-query latency is **monotonically increasing** across runs and consistently exceeds budget on the last two. This is no longer plausibly "Gemini jitter" — it looks like a structural latency in `services.parse_query` that the spec budget cannot accommodate.

### Stopped early (no Personas 2-3 / no edge cases)

Per Rules ("No retries on flaky steps. If a step fails, hard-fail and report."): Personas 2 (Sustainable Korean) and 3 (Bare Query), Steps B5-B8 not executed.

### Cleanup
- `test-artifacts/review/` removed.

### Verdict
- **`BROWSER TEST: FAIL — Persona Brutalist time-to-first-card 6706 ms > 4000 ms hard ceiling (parse-query latency alone exceeds budget; second consecutive run failing on this gate, latency monotonically increasing across the last 3 runs).`**

### Decision needed at the workflow level

This branch (`f607e73`) is **PASS on Part A and on every code-correctness axis**. The IMP-1 fix landed cleanly. But Part B blocks push **for the second time in a row on a metric that has nothing to do with the changes in this branch** — `farthest_point_from_pool` (the `select_ms` portion of swipe latency) is on the swipe hot path, not the time-to-first-card path. The IMP-1 perf gain doesn't address the parse-query latency at all.

This puts the workflow in a knot: every batch that touches UI-affecting paths will FAIL Part B on parse-query latency until either:

1. **`services.parse_query` is optimized** — investigation 01 (`research/investigations/01-swipe-latency-feasibility.md`) is referenced in the spec v1.1 SPEC-UPDATED note as a 4-step optimization path. **Until this lands, every UI-affecting branch will hit the same wall.**
2. **Spec budget is loosened** — the requirements.md §4 budget of <3-4s was set when parse-query was around 3.4s. Three runs later the floor appears to be 5-7s. If 5-7s is the new normal post-Gemini-API-changes, the budget should rise to <8s (a clear margin above observed) until #1 lands.
3. **Part B latency gate is made tier-aware** — fail on the swipe-loop latency (the actual hot path the user feels) but warn-only on the parse-query latency until #1 lands. The swipe-loop test is what `a9305e4`'s IMP-1 fix is supposed to improve; gating Part B on parse-query masks that signal.

**My recommendation**: Option 3 (tier-aware gate) is the lowest-friction, highest-fidelity path right now. Option 2 is a band-aid. Option 1 is the real fix but takes time. **Out of scope for this branch — but the workflow itself needs a decision before the next UI-affecting batch tries to push.**

## References

- `.claude/Goal.md`: Vision + Target Personas
- `.claude/Report.md`: Backend Structure (engine.py row notes IMP-1 max-min correctness + vectorization; tests row bumped to 44); Last Updated (Claude); Gemini section preserved
- `.claude/Task.md`: Handoffs (REVIEW-FAIL: 5d85b90 from prior, REVIEW-REQUESTED for 2f8a943 + a9305e4); Resolved (CONV1, POOL1, DISLIKE1, SCHEMA1, IMP1)
- `.claude/WORKFLOW.md`: Agent Roster + Pre-Push Review Gate (unchanged)
- `.claude/commands/review.md`: read previously; Step B1bb backstop verified again
- `.claude/agents/orchestrator.md`: Step 2.5 (app-agnostic showmigrations after `2f8a943`)
- `.claude/agents/back-maker.md`: "After writing > 2. Apply migrations" (unchanged from `5d85b90`)
- `.claude/agents/reporter.md`: Step 6 narrow-exception (4 sub-steps + hard limits)
- `CLAUDE.md`: Pre-Push Review (`/review`) section + Natural language trigger + research read-only
- `research/spec/requirements.md` v1.1: §11.1 IMP-1 directive (consulted via SPEC-UPDATED handoff entry)
- `research/algorithm.md`: full diff — Last Synced + 4 inline annotations
- `research/investigations/02-farthest-point-correctness-and-vectorization.md`: full read — verified the spec example geometric error caught by implementer
- `backend/apps/recommendation/engine.py:421-455`: full read of refactored `farthest_point_from_pool` — math verified
- `backend/tests/test_sessions.py:875-936`: full read of `TestFarthestPointFromPool` (5 tests including geometric counterexample)
- Empirical: `git log origin/main..HEAD --diff-filter=AMD --name-only -- research/` — confirms only `algorithm.md` touched in main pipeline
- Empirical: `git show a9305e4 --stat` + `git show 2f8a943 --stat` + `git show f607e73 --stat` — surgical scopes confirmed
- Prior reviews: `.claude/reviews/88f0532.md` and `.claude/reviews/5d85b90.md` (postmortem chain that this branch closes)
